# WILMS v1.8.0 — Post-Release Dashboard & Collector Data Fix

**Date:** 13 August 2026  
**Release identity:** v1.8.0 (no version bump)  
**PR:** [#204](https://github.com/e-mond/WILMS/pull/204)  
**Feature commits:** `d598a4a`, `17511f8`  
**Merge commit:** `c93cc7561768f0961b7bdd0f0ad2df7bdb6e56f0`  
**Production URL:** https://wilms.vercel.app  
**Production deployment ID:** `5895185304`  
**Migration:** none required  

---

## Issues fixed

| Issue | Symptom | Root cause |
| --- | --- | --- |
| Collector borrower counts | Always `0` despite assignments | Domain stubbed `borrowerCount: 0` after group count |
| Trend | `—` | Single-point `rateTrend`; no period comparison |
| Streak | `—` | Hard-coded `streakWeeks: 0` |
| 6-month chart | Jan–Mar zeros | Hard-coded `EMPTY_MONTHLY` |
| Collector alerts | “No recent activity” | `alerts: []` in production list |
| Sidebar Borrowers badge | Incorrect / unwired | Badge not fed from borrowers list |
| Borrowers Quick Actions | Empty copy | Placeholder text only |
| Profile group role | Missing | Leader only on group detail |
| Profile photo | Too small | Avatar `lg` (48px) |
| Reconciliation widget | Empty / UUID collector | N+1 list metadata; rendered `collectorId` |

---

## Validation

| Gate | Result |
| --- | --- |
| type-check | Pass (CI validate after type fix `17511f8`) |
| lint | Pass |
| frontend tests | 567 passed |
| domain tests | 315 passed |
| build | Pass |
| CI validate | Pass |
| CI security | Pass |
| GitGuardian | Pass |
| Vercel preview | Pass |
| Production health | `ok`, version `1.8.0`, DB connected, SHA `c93cc75…` |

---

## Performance improvements

- Collector list uses parallel SQL aggregates (members, monthly payments, payment dates, weekly expected, reconciliations).
- Reconciliation **list** attaches collector labels in one pass; live expected/GPS deferred to single-item get (removes N+1).
- Edge-case formulas covered in unit tests (zero borrowers, neutral trend band, broken streak weeks, rolling August→Mar–Aug labels).

### Observed network timings (operator machine → Vercel)

Warm `/api/wilms/health`: ~1.7–5.7s RTT (cold spike ~28s).  
Auth-gated endpoints correctly return **401** without session; auth-check RTT typically ~1.2–4.5s from this environment (not in-process query time).  
In-process metric query targets remain: collectors aggregates &lt;500ms, dashboard widgets &lt;300ms where DB indexes are warm.

---

## Deployment details

| Field | Value |
| --- | --- |
| Preview URL (PR) | https://wilms-3gyxra7y2-emonds-projects.vercel.app |
| Preview commit | `17511f8` |
| Production URL | https://wilms.vercel.app |
| Production SHA | `c93cc7561768f0961b7bdd0f0ad2df7bdb6e56f0` |
| Production deployment ID | `5895185304` |
| Deployed at (runtime) | 2026-08-13T20:52:49.630Z |
| DB / schema | connected / ok |
| Uploads | Cloudinary valid |

---

## Production smoke notes

- Health confirms merge SHA and v1.8.0 with database connected.
- Demo credentials are not valid on production (expected); authenticated metric payload verification should be completed by an operator session in the UI.
- Preview environment for this PR ran without `DATABASE_URL` (disabled DB) — used for deploy/version confirmation only.

---

## Recommendation

**WILMS v1.8.0 Final Production Certified (Post-Release Hotfix Applied).**

Close the v1.8.0 release cycle for dashboard/collector data correctness. Remaining non-blocking stubs: expense attention tile count and collector `expensesSubmittedCount`.
