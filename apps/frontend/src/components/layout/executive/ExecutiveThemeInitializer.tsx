'use client';

/**
 * Previously forced dark mode on first executive visit.
 * Theme preference is now fully controlled by ThemeProvider + user toggle.
 */
export function ExecutiveThemeInitializer() {
  return null;
}
