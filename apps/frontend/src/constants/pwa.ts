export const PWA_PAYMENT_SYNC_TAG = 'wilms-payment-sync';

export const PWA_SW_MESSAGE_TYPE = 'WILMS_PAYMENT_SYNC';

/** Client → service worker: enable Phase 5 navigate shell fallback when offline mode is on. */
export const PWA_SW_SET_OFFLINE_MODE = 'WILMS_SET_OFFLINE_MODE';

export const PWA_INSTALL_DISMISS_KEY = 'wilms-pwa-install-dismissed';

/** Soft-dismiss for the default push subscribe banner (Super Admin / installed PWA). */
export const PWA_PUSH_PROMPT_DISMISS_KEY = 'wilms-push-subscribe-prompt-dismissed';

export const PWA_SW_PATH = '/sw.js';
