/** Default variance threshold — AMB-004 resolution (configurable in settings future). */
export const RECONCILIATION_VARIANCE_THRESHOLD_PERCENT = 10;

/** Matches backend MIN_FLAGGED_COMMENT_LENGTH in reconciliation service. */
export const RECONCILIATION_FLAGGED_COMMENT_MIN_LENGTH = 10;
