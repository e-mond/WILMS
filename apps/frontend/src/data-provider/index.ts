export {
  DEMO_MODE_MESSAGE,
  isDemoMode,
  resolveDataProviderMode,
  type DataProviderMode,
  type IDataProvider,
} from '@/data-provider/types';

export { mockDataProvider } from '@/data-provider/MockDataProvider';
export { apiDataProvider } from '@/data-provider/ApiDataProvider';
