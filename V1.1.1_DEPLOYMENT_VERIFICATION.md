# WILMS v1.1.1 Deployment Verification

**Date:** 2026-07-05  
**Branch verified (local):** `hotfix/v1.1.1-production-fixes` @ `8dcda6e`  
**Production Railway commit:** `d2a64bb` (v1.1 — hotfix **not yet merged/deployed**)

---

## Railway (`https://wilms-production.up.railway.app`)

| Check | Result | Evidence |
|-------|--------|----------|
| HTTP `/health` | **200** | `status=ok` |
| `schema.status` | **ok** | `missingTables=0` |
| Database | **connected** | `database.connected=true` |
| Cloudinary | **configured** | `uploads.cloudinaryConfigured=true`, `activeProvider=cloudinary` |
| Migrations | **13/13** | `expected=13 applied=13 status=ok` |
| `gitCommit` | `d2a64bbf5c5015cfde9598d942429da67d7d7323` | Matches deployed v1.1 merge |
| API `version` | `1.0.0` | Health payload (package bump to 1.1.1 pending Railway deploy) |
| Environment | `production` | `node=v20.20.2` |
| JWT / session | configured | `session.provider=hmac-signed-token` |

---

## Vercel (`https://wilms.vercel.app`)

| Check | Result | Evidence |
|-------|--------|----------|
| Login page | **200** | Production smoke `frontend-login-page` |
| Version label | **WILMS v1.1.1** | `npm run verify:version` login page check |
| BFF proxies | **200** | dashboard, borrowers, loans, groups, reports, settings, collectors |
| CSRF | **enforced** | token issued; login without token → 403 |
| Auth session | **ok** | login smoke passes |

**Note:** Vercel frontend appears ahead of Railway API (login shows v1.1.1; API health still reports v1.0.0 and commit `d2a64bb`). Full v1.1.1 parity requires merging hotfix PR and redeploying **both** services.

---

## Deploy sync

```bash
# Against current production (v1.1):
WILMS_API_URL=https://wilms-production.up.railway.app \
EXPECTED_GIT_COMMIT=d2a64bbf5c5015cfde9598d942429da67d7d7323 \
npm run verify:deploy-sync
# PASS

# Against hotfix (after merge + deploy):
EXPECTED_GIT_COMMIT=8dcda6ed675aab2e276851f46a1cc18c918bb787 \
npm run verify:deploy-sync
# Pending hotfix deployment
```

---

## Smoke tests (current production)

| Suite | Result |
|-------|--------|
| `smoke:production` | **32/32 PASS** (with `WILMS_EXPECTED_GIT_COMMIT=d2a64bb`) |
| `smoke:rbac` | **11/11 PASS** |

Local `.env` had stale `WILMS_EXPECTED_GIT_COMMIT`; CI uses `github.sha` and is unaffected.

---

## Post-hotfix deploy expectations

After merging `hotfix/v1.1.1-production-fixes` and deploying:

- Migrations **14/14** (adds `0013_v111_hotfixes`)
- API health `version=1.1.1`
- `gitCommit=8dcda6e…`
- Hotfix features live (disburse UI, `DIS-*` IDs, audit labels, navigation, etc.)
