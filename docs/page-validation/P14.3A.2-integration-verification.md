# P14.3A.2 — Integration Verification

**Phase:** P14.3A.2 Phase 4  
**Date:** 2026-06-09  
**Status:** Complete (architecture verified; live stack partially blocked)

---

## Integration Chain

```text
Frontend (Next.js)
    ↕  apiClient → NEXT_PUBLIC_API_BASE_URL
    ↕  (optional) /api/wilms BFF → WILMS_API_UPSTREAM
Backend (Express @ :4000)
    ↕  Drizzle + Neon when DATABASE_URL set
Neon PostgreSQL
```

---

## Response Envelopes

| Layer | Format | Status |
|-------|--------|--------|
| Backend success | `{ data: T }` via `sendData()` | **Working** |
| Backend errors | `{ error: { message, code } }` | **Working** |
| Frontend unwrap | `apiClient` strips `data` | **Working** |
| Health check | `{"data":{"status":"ok","service":"wilms-api"}}` | **Working** (runtime) |

---

## Auth Flow

| Step | Integration | Status |
|------|-------------|--------|
| Login form → `/api/auth/login` | Next route sets session cookie | **Working** (mock auth in dev) |
| BFF → backend | Cookie → Bearer header | **Verified** (code) |
| Protected backend routes | `requireAuth` + permissions | **Working** — `/loans` → 401 without token |
| Production API auth | Depends on backend session validation | **Partially integrated** |

---

## Domain Integration Matrix

| Domain | Frontend service | Backend route | Neon persistence | Classification |
|--------|------------------|---------------|------------------|----------------|
| Auth | `authService` | `/auth/*` | users table | **Partially integrated** — demo tokens |
| Borrowers | `borrowerService` | `/borrowers` | ✅ when DB on | **Partially integrated** |
| Loans | `loanService` | `/loans/*` | ✅ when DB on | **Partially integrated** |
| Payments | `paymentService` | `/payments` | ✅ when DB on | **Partially integrated** |
| Groups | `groupService` | limited backend | partial | **Mock-backed** (UI) |
| Group formation | `groupFormationService` | `/group-formation` | partial | **Partially integrated** |
| Reports | `reportService` | `/reports` | payment-derived | **Partially integrated** |
| Uploads | `uploadService` | `/uploads` | filesystem + DB | **Partially integrated** |
| Audit | `auditService` | `/audit` | ✅ | **Partially integrated** |
| Dashboard | `dashboardService` | no dedicated API | — | **Mock-backed** |
| Collectors | `collectorService` | no dedicated API | — | **Mock-backed** |
| Loan pools | `loanPoolService` | ❌ none | — | **Mock-backed** |
| Adjustments | `adjustmentService` | ❌ none | — | **Mock-backed** |
| Reconciliation | `reconciliationService` | ❌ none | — | **Mock-backed** |
| Reversals | via adjustments/transactions | ❌ none | — | **Not integrated** |
| Write-offs | loan status UI | lifecycle enum only | — | **Not integrated** |
| Settings | `settingsService` | ❌ none | — | **Mock-backed** |
| Notifications | `notificationService` | ❌ none | — | **Mock-backed** |
| Risk flags | `riskFlagService` | ❌ none | — | **Mock-backed** |
| Search | `searchService` | ❌ none | — | **Mock-backed** |

---

## Specific Flow Checks

### Borrower retrieval

| Check | Result |
|-------|--------|
| Production `borrowerService` uses `apiClient.get('/borrowers')` | **Verified** (code) |
| Backend `borrowersRouter` exists | **Verified** |
| Live E2E through Neon | **Blocked** — dev mock mode + seed failure |

### Payment posting

| Check | Result |
|-------|--------|
| Production `paymentService` → `POST /payments` | **Verified** (code) |
| Backend idempotency + ledger | **Verified** (code + unit harness) |
| Live posting to Neon | **Blocked** — seed + env |

### Report retrieval

| Check | Result |
|-------|--------|
| Frontend report pages | **Mock-backed** in dev |
| Backend `/reports` list + payment exports | **Partially integrated** |
| Full report parity with mock | **Not integrated** |

### Upload retrieval

| Check | Result |
|-------|--------|
| Backend `uploadsRouter` | **Verified** |
| Frontend `uploadService` production impl | **Verified** (code) |
| Live upload E2E | **Not tested** this session |

---

## Default Development Integration State

```text
Frontend (mock services)
    ✗  no connection to backend for business data
Backend (:4000, likely in-memory)
    ✗  not consumed by frontend in dev
Neon
    ✓  reachable for migrations
    ✗  no seeded financial data
```

---

## Production Integration Path (documented, not verified live)

1. Set `DATABASE_URL`, fix seed, run migrate + seed
2. Run backend with Neon: `[postgresql (Neon)]` in startup log
3. Set `NEXT_PUBLIC_API_BASE_URL` to BFF or backend URL
4. Set `WILMS_API_UPSTREAM=http://127.0.0.1:4000`
5. Run frontend with `NODE_ENV=production` build/start

---

## Summary

| Integration tier | Count | Domains |
|------------------|-------|---------|
| **Working** (runtime evidence) | 2 | Health endpoint, auth rejection (401) |
| **Partially integrated** | 8 | Auth, borrowers, loans, payments, groups, reports, uploads, audit |
| **Mock-backed** | 10+ | Dashboard, collectors, pools, adjustments, reconciliation, settings, etc. |
| **Not integrated** | 3 | Reversals, write-off processing, full financial controls |
