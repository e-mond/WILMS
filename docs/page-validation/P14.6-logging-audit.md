# P14.6 — Logging Audit

**Release:** v0.2.2  
**Branch:** `release/p14.6-cd-sync-ux`  
**Date:** 2026-06-30

---

## Policy

Production runtime uses structured logger (`apps/backend/src/infrastructure/logging/logger.ts`). `console.log` is permitted only in verification/certification scripts and one-off operator tools — not in request handlers or services.

---

## `console.log` Inventory

| Category | Paths | Count | Production risk |
|----------|-------|-------|-----------------|
| Verification harnesses | `apps/backend/src/verification/*.ts` | ~40 files | **None** — CLI-only, not imported at runtime |
| DB seed | `apps/backend/src/db/seed/*.ts` | 3 | **None** — migration/seed CLI |
| Operator scripts | `apps/backend/scripts/*.mjs`, `rotate-production-users.mjs` | 4 | **None** — manual execution |
| Budget scripts | `scripts/bundle-budget-check.mjs`, `perf-budget-check.mjs` | 2 | **None** — CI only |
| Structured logger | `logger.ts` L29 | 1 | **Acceptable** — JSON line to stdout |
| Frontend proxy smoke | `apps/frontend/scripts/proxy-reconciliation-smoke.mjs` | 1 | **None** — dev tool |
| P14.3C inventory | `scripts/p14.3c/extract-inventory.mjs` | 1 | **None** — one-off |

**Application code (routes, services, middleware):** zero `console.log` calls.

---

## v0.2.2 Changes

| File | Logging |
|------|---------|
| `mock-guard.ts` | `logger.error('startup.mockFlagInProduction')` — structured |
| `sync/service.ts` | No console output |
| `sync/routes.ts` | Errors via `AppError` → error handler |

---

## Verdict

**PASS** — `console.log` confined to verification scripts and CI tooling. Production paths use structured logger.
