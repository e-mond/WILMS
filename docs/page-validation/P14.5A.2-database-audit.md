# P14.5A.2 — Database Audit

**ORM:** Drizzle 0.38  
**Migrations:** `apps/backend/src/db/migrations/` — journal entries **7** (`0000`–`0006`)

---

## Migration inventory

| File | Purpose |
|------|---------|
| `0000_init.sql` | Core schema: users, borrowers, loans, payments, schedules, ledger, groups, audit |
| `0001_tired_sir_ram.sql` | Incremental |
| `0002_sad_bucky.sql` | Incremental |
| `0003_loan_pools.sql` | Loan pools |
| `0004_financial_adjustments.sql` | Adjustments |
| `0005_financial_reversals.sql` | Reversals |
| `0006_financial_reconciliations.sql` | Reconciliations |

Health check reads `__drizzle_migrations` count vs journal (`health.service.ts` L82–88).

---

## Schema tables (`db/schema/index.ts`)

| Table module | Used by repository/service | Status |
|--------------|---------------------------|--------|
| users, rbac | userRepository, auth | ✅ Active |
| borrowers | borrowerRepository, borrowerService | ✅ Active |
| loans, loan_schedules, loan_disbursements | loanRepository, loanService | ✅ Active |
| payments, ledger_entries | paymentService, ledgerRepository | ✅ Active |
| idempotency_keys | idempotency.repository | ✅ Active |
| loan_pools | loan-pool.repository | ✅ Active |
| financial_adjustments | adjustment service | ✅ Active |
| financial_reversals | reversal service | ✅ Active |
| financial_reconciliations | reconciliation service | ✅ Active |
| groups, group_members | group.repository, formation, cert prep | ✅ Partial — no CRUD API |
| uploads | upload.repository | ✅ Active |
| audit | audit-log (in-memory + optional persist) | ✅ Active |
| expenses | **schema only** | ⚠️ **Orphan** — no routes |
| notifications | **schema only** | ⚠️ **Orphan** — no routes |

---

## Seed & certification reset

| Script | Purpose | Deterministic |
|--------|---------|---------------|
| `db/seed/seed-financial.ts` | Four canonical seed loans | ✅ |
| `cert-financial-prep.ts` | Reset seed loans + idempotency cleanup | ✅ P14.5A.1 |
| `cert-live-prep.ts` | Cert borrower + harness loan cleanup | ✅ P14.5A.1 |
| `cert-reversal-seed-reset.ts` | Reversal harness pollution | ✅ |

Shared Neon pollution mitigated by seed-scoped cert checks (P14.5A.1).

---

## Enums

Centralized in `db/schema/enums.ts` and mirrored in `@wilms/shared-contracts`. Financial enums (loan status, schedule status, reversal types) align with domain logic in `domain/loan/lifecycle.ts`.

---

## Persistence dual-mode

| Mode | Trigger | Production suitability |
|------|---------|---------------------|
| PostgreSQL | `DATABASE_URL` set | ✅ Required production |
| In-memory | `DATABASE_URL` unset | ❌ Dev/demo only — `db/persistence.ts`, payment fallbacks |

`validate-env.ts` errors if `DATABASE_URL` missing in `NODE_ENV=production`.

---

## Foreign key cleanup (cert harness)

Cert prep scripts delete in order: reversals → adjustments → pool allocations → ledger → payments → schedules → disbursements → loans (`cert-financial-prep.ts`). Verified idempotent on double-run (P14.5A.1).

---

## Database risks

| ID | Issue | Severity |
|----|-------|----------|
| DB-001 | `expenses` + `notifications` tables migrated but no API — schema drift | Medium |
| DB-002 | Shared Neon accumulates non-seed rows — cert scoped, not prod isolation | Medium |
| DB-003 | `cert-upload-smoke` uses invalid UUID for `entityId` column | High (cert) |
| DB-004 | No automated migration run in CI visible from repo | Low |

No database changes in this audit.
