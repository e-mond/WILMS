# P14.3A.1 — Idempotency Audit

**Phase:** P14.3A.1 Phase 4  
**Date:** 2026-06-09  
**Status:** Complete (code verified; DB replay tests require DATABASE_URL)

---

## Execution Status

Disbursement idempotency replay: **NOT EXECUTED** locally. Unique-index race fix verified in `idempotency.repository.ts`.

---

## Scope

| Operation | Header | Scope enum | Wrapper |
|-----------|--------|------------|---------|
| Loan disburse | `Idempotency-Key` | `LOAN_DISBURSE` | `runWithIdempotency()` |
| Payment POST | `Idempotency-Key` | `PAYMENT_POST` | `runWithIdempotency()` |

Unique constraint: `(scope, actor_user_id, idempotency_key)` on `idempotency_keys`.

Retention: 24 hours (`RETENTION_MS` in repository).

---

## Disbursement — Duplicate Completed Request

**Test:** `runIdempotencyChecks()` — two `disburseLoan()` calls with same key on pending seed loan.

**Expected:**

* One disbursement record
* Second call returns identical `LoanDetailDto.id`
* One LOAN_DISBURSEMENT ledger entry

**Evidence:** `db-checks.ts` checks `disburse-idempotency-replay`, `disburse-single-record`.

---

## Repayment — Duplicate Completed Request

**Mechanism:** `beginIdempotency` → execute → `completeIdempotency` stores response body.

Replay returns stored body without re-executing `postPayment()`.

**Additional guard:** `findDuplicatePayment()` on (borrowerId, paymentDate, amountPesewas) before posting.

---

## In-Flight Requests

When key exists with `state = IN_PROGRESS`:

```text
throw new Error('IDEMPOTENCY_IN_PROGRESS')
→ HTTP 409 CONFLICT
```

**Evidence:** `idempotency.repository.ts` lines 37–39; `mapFinancialRouteError()`.

---

## Race Condition — Concurrent Same Key

**Scenario:** Two parallel requests insert same idempotency key.

**Before P14.3A.1:** Second insert could throw unhandled PostgreSQL 23505 → HTTP 500.

**After P14.3A.1:** Unique violation caught → re-read row → `IDEMPOTENCY_IN_PROGRESS` or replay.

**Evidence:** `isUniqueViolation()` + `resolveExistingIdempotency()` in `idempotency.repository.ts`.

---

## Failure Cleanup

On execute failure, IN_PROGRESS key deleted so client can retry with same key.

**Evidence:** `run-with-idempotency.ts` catch block.

---

## Verification

```bash
DATABASE_URL=… npm run verify:financial -w @wilms/api
```

Unit/security checks (no DB): 18/18 passed including RBAC 409 paths.

---

## Findings

| ID | Finding | Severity | Action |
|----|---------|----------|--------|
| ID-001 | Unique-index race → 500 | **Defect → Fixed** | resolveExistingIdempotency |
| ID-002 | No idempotency on loan create | Info | LOAN_CREATE scope defined but unused; acceptable |

No duplicate financial record path identified post-fix.
