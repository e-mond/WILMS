# P14.6.4 — Monitoring Report

**Date:** 2026-06-30  
**Status:** **DOCUMENTED** — health probes verified locally; dashboard access not in scope

---

## Monitoring surfaces

| Surface | Endpoint / tool | Repo reference |
|---------|-----------------|----------------|
| API health | `GET /health` | `apps/backend/src/modules/health/routes.ts` |
| Health service | Build ID, migrations, DB, uploads | `apps/backend/src/modules/health/health.service.ts` |
| Version consistency | `scripts/verify-version-consistency.mjs` | Invoked in `deploy-production.yml` |
| Production smoke | `npm run smoke:production` | `package.json` → `@wilms/api` |
| Staging smoke | `npm run smoke:staging` | `deploy-staging.yml` |
| API matrix | `scripts/p14-6-4-api-matrix.mjs` | CI-adjacent static check |

---

## Health check contract

`GET /health` returns (via `buildHealthReport()`):

| Field | Purpose |
|-------|---------|
| `status` | `ok` / degraded |
| `version` | Package version (`0.2.2`) |
| `gitCommit` | Deploy SHA drift detection |
| `database.connected` | Neon connectivity |
| `migrations.applied` / `migrations.total` | Schema drift (expect **8/8** after `0008_admin_extensions.sql`) |
| `uploads.valid` | Cloudinary config |
| `uptimeSeconds` | Process uptime |

**Production URL:** `${WILMS_API_URL}/health` (Railway)  
**Frontend:** `https://wilms.vercel.app` (Vercel)

---

## Railway monitoring

| Signal | P14.6.3 baseline | P14.6.4 expectation |
|--------|----------------|---------------------|
| Deploy ID | `045ad513-3c5d-404b-b3a9-c1cf7efd83c8` | New deploy after P14.6.4 merge |
| `gitCommit` on `/health` | `f69427c` (non-null) | Must match deployed SHA |
| Migrations | 8/8 | 8/8 (includes `0008_admin_extensions.sql`) |
| Version | 0.2.2 | 0.2.2+ |

**Recommendations** (from `P14.6.2-monitoring-report.md`, still applicable):

1. Enable Railway deploy failure notifications.
2. Set `WILMS_GIT_COMMIT` on every deploy.
3. Alert when `migrations.applied < migrations.total`.
4. Review Railway logs after P14.6.4 API module deploy.

---

## Vercel monitoring

| Signal | Check |
|--------|-------|
| Production alias | `wilms.vercel.app` → latest `--prod` deployment |
| BFF auth routes | `/api/auth/login`, `/api/auth/csrf` |
| Function errors | Vercel dashboard (not read this session) |
| Build | Triggered via `deploy-production.yml` or manual `vercel --prod` |

P14.6.3 confirmed Vercel on v0.2.2 with BFF auth fix — see `P14.6.3-production-acceptance.md`.

---

## CI/CD post-repair monitoring

After workflow repair (`P14.6.4-workflow-repair.md`):

| Workflow | Trigger | Post-deploy check |
|----------|---------|-------------------|
| `deploy-staging.yml` | CI success + `ENABLE_STAGING_DEPLOY=true` | `curl` staging health + `smoke:staging` |
| `deploy-production.yml` | Manual `confirm=deploy` | Health + version script + `smoke:production` |

---

## Gaps (not configured in repo)

| Capability | Status |
|------------|--------|
| External uptime probe | Not in repo |
| Railway log drains | Not configured |
| Vercel log drains | Not configured |
| 404 rate alerting | Not configured |
| Neon / Cloudinary dashboards | Operator access only |

---

## Verdict

**CONDITIONAL PASS** — Health and smoke hooks exist and are wired into deploy pipelines. Operational dashboards not verified this session. Run post-deploy: `curl $WILMS_API_URL/health` → confirm `migrations: 8/8`, `version: 0.2.2`, non-null `gitCommit`.
