# P14.3B Phase 5B — Security Certification

**Date:** 2026-06-20  
**Branch:** `feature/p14.3b-phase-5b-production-certification` @ `a915fe6` (+ C-fix)  
**Status:** **PASS** (application controls) / **PARTIAL** (infrastructure & deferred items)

Prior audit: [`docs/audit/P14.3B-security-audit.md`](../audit/P14.3B-security-audit.md) — re-verified at same HEAD family post `b68f3eb` fixes.

---

## Results

| C-ID | Check | Method | Result | Evidence |
|------|-------|--------|--------|----------|
| C-040 | HMAC-SHA256 sessions | Forge unsigned token → API | **PASS** | `unsigned-forged-token-blocked` → 401 |
| C-040a | Expired session | Tampered `expiresAt` | **PASS** | `expired-session-blocked` → 401 |
| C-041 | bcrypt password storage | Post `db:seed` | **PASS** | `password_hash` prefix `$2` |
| C-042 | Login rate limiting | 25 failed attempts | **PASS** | `login-rate-limit-triggered` → 429 |
| C-043 | RBAC — loans | Role matrix via harness | **PASS** | 11/11 security checks |
| C-043a | RBAC — reconciliation | `cert:reconciliation:rbac` | **PASS** | 6/6 |
| C-043b | RBAC — audit POST | Collector denied, auditor allowed | **PASS** | `collector-cannot-post-audit` / `auditor-can-post-audit` |
| C-044 | Cookie `httpOnly` | `cookies.ts` | **PASS** | `httpOnly: true` |
| C-044a | Cookie `secure` in production | `NODE_ENV=production` | **PASS** | `secure: process.env.NODE_ENV === 'production'` |
| C-044b | Cookie `sameSite` | `cookies.ts` | **PASS** | `sameSite: 'lax'` (strict not used — documented) |
| C-045 | HTTPS enforcement | App / proxy | **FAIL/WARNING** | Local HTTP; F-S009 — proxy responsibility |
| C-046 | Health + DB connectivity | `GET /health` | **FAIL** | Returns `{status:ok}` only — no DB probe |
| C-047 | Audit API immutability | Route scan | **PASS** | No DELETE on audit routes |
| C-048 | Upload IDOR guard | Post F-S006 fix | **PASS** | Owner check in `uploads/routes.ts` |
| C-049 | 500 error sanitization | `error-handler.ts` | **PASS** | Generic message in production |
| C-049a | CORS single origin | Config | **PASS** | `WILMS_CORS_ORIGIN` |
| C-049b | Reconciliation approval endpoint | Design review | **N/A** | v1 collector submit only — no approval API |

---

## Security harness output (re-verification)

From `npm run verify:financial -w @wilms/api` security section:

```
Passed: 11/11
  ✓ unauthenticated-loans-blocked
  ✓ expired-session-blocked
  ✓ unsigned-forged-token-blocked
  ✓ collector-cannot-approve-loan
  ✓ collector-cannot-disburse
  ✓ officer-cannot-post-payment
  ✓ malformed-loan-create-rejected
  ✓ collector-cannot-reverse-payment
  ✓ collector-cannot-post-audit
  ✓ auditor-can-post-audit
  ✓ login-rate-limit-triggered
```

Evidence: [`phase-5b-evidence/verify-financial.txt`](phase-5b-evidence/verify-financial.txt)

---

## Deferred / Medium findings (human deferral accepted for 5B)

| ID | Item | Status |
|----|------|--------|
| F-S007 | OTP not implemented | Documented — not 5B scope |
| F-S008 | Password reset not implemented | Documented |
| F-S011 | Helmet headers | Documented — proxy or 5B+ |
| F-S012 | Group-formation RBAC gap | Documented |

---

## Failures & blockers

| Severity | Finding | Mitigation |
|----------|---------|------------|
| Medium | HTTPS not at app layer | Terminate TLS at reverse proxy |
| Medium | Health does not detect DB outage | Add DB ping or external monitor |
| Low | Login audit maps to `SETTINGS_EXPORTED` enum | Schema migration deferred (F-S013) |

---

## Summary

All **Critical** and **High** security fixes from pre-5B audit **re-verify PASS** on local Neon. No new Critical/High regressions found. Production certification is **blocked** on infrastructure items (HTTPS, health DB probe, Cloudinary) rather than auth/RBAC logic.
