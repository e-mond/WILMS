# P14.3B — Stakeholder Demo Script

**Phase:** 4D.2 Demo Certification  
**Date:** 2026-06-26  
**Audience:** Business stakeholders, operations leads  
**Duration:** ~25 minutes (20 min demo + 5 min Q&A)  
**Environment:** Local stack — backend `:4000`, frontend `:3000`, Neon DB, local upload provider

---

## Pre-Demo Checklist (15 minutes before)

```bash
# Terminal 1 — backend
npm run start -w @wilms/api

# Terminal 2 — frontend (MUST restart after .env.local changes)
npm run dev -w @wilms/frontend
```

| Check | Required state |
|-------|----------------|
| `GET http://127.0.0.1:4000/health` | 200 |
| `apps/frontend/.env.local` | `NEXT_PUBLIC_USE_MOCK=false` |
| Frontend restarted after env change | **Yes** |
| Proxy smoke (optional) | `node apps/frontend/scripts/proxy-reconciliation-smoke.mjs` → 5/5 |

**Credentials:** See demo user table in `P14.3B-phase-4d2-environment.md`.

---

## Demo Sequence (13 Steps)

### Step 1 — Opening & system context
**Time:** 2 min | **Role:** Presenter

- State purpose: live Women's Interest-Free Loan Management System on certified backend.
- Note: API evidence primary; browser walkthrough is rehearsal — capture screenshots here if needed.
- Confirm both servers running and live API mode active.

---

### Step 2 — Super Admin login
**Time:** 1 min | **Role:** Super Admin (`admin@wilms.demo`)

- Navigate to `http://127.0.0.1:3000/login`.
- Sign in as Super Admin.
- **Show:** Dashboard loads; session cookie set (no mock data banner if live mode correct).
- **API evidence:** `authentication/login-admin` PASS (~1253 ms first login).

---

### Step 3 — Borrower search
**Time:** 2 min | **Role:** Super Admin

- Open borrowers list; search for a name (e.g. "Ama" or "Akosua").
- **Show:** Search returns approved borrowers with group names.
- **Talking point:** Registration approval workflow; approved borrowers eligible for loans.
- **API evidence:** `borrower/search` PASS, 197 ms.

---

### Step 4 — Borrower profile
**Time:** 1 min | **Role:** Super Admin

- Open borrower detail (e.g. Akosua Mensah, Sunrise Group).
- **Show:** ID type, phone, community, registration date, status APPROVED.
- **API evidence:** `borrower/profile` PASS, 201 ms.

---

### Step 5 — Loan lifecycle (create → approve → disburse)
**Time:** 3 min | **Role:** Loan Approver (`approver@wilms.demo`)

- **If inline create works:** Create loan for eligible borrower → approve → disburse.
- **If seed exhausted (expected on shared Neon):** Explain validation — "borrower already has active/pending loan." Show existing active loan schedule instead.
- **Fallback narrative:** Same-session `verify:live` certified create → approve → disburse → payment (23/23).
- **API evidence:** Inline `loan/create` **FAIL**; carried `verify:live workflow-create-loan` **PASS**.

---

### Step 6 — Collection recording
**Time:** 2 min | **Role:** Collector (`collector@wilms.demo`)

- Switch to Collector account (logout → login, or second browser profile).
- Record a field payment against an active loan (collection sheet or payment form).
- **Show:** Full weekly amount; GPS capture if on collection sheet route.
- **Fallback:** Reference `verify:live` `workflow-post-repayment` if no fresh loan available.
- **Talking point:** No partial payments; oldest obligation cleared first (BRD §9.1).

---

### Step 7 — Reconciliation summary
**Time:** 2 min | **Role:** Collector

- Navigate to `/collector/reconciliation`.
- Select a date with zero expected collections (or use cert date pattern).
- **Show:** Expected due, system recorded, physical cash preview — variance formula matches backend (`physical − expected`).
- **API evidence:** `reconciliation/fetch-summary` PASS, 939 ms.

---

### Step 8 — Submit zero-variance reconciliation
**Time:** 2 min | **Role:** Collector

- Enter physical cash matching expected due; submit.
- **Show:** Success confirmation; submitted state.
- **Talking point:** Flagged variance requires ≥10 character comment (4D.1 UI fix).
- **API evidence:** `reconciliation/submit-zero-variance` PASS, 1550 ms.

---

### Step 9 — Duplicate submission blocked
**Time:** 1 min | **Role:** Collector

- Attempt second submit for same date.
- **Show:** Validation error — "Reconciliation already submitted for this date."
- **API evidence:** `reconciliation/duplicate-prevention` PASS, HTTP 422.

---

### Step 10 — Document upload (local provider)
**Time:** 2 min | **Role:** Super Admin or Officer

- Upload borrower photo or registration attachment.
- **Show:** Upload succeeds; preview loads from `/uploads/{id}/content`.
- **Disclose:** Cloudinary **not configured** — local disk + PostgreSQL for demo; production will use CDN when creds set.
- **API evidence:** All upload steps PASS; `uuidv7()` ID fix in `upload.service.ts`.

---

### Step 11 — Payment reversal
**Time:** 3 min | **Role:** Super Admin

- Open payment requiring reversal; execute reversal with reason.
- **Show:** Balance restored; duplicate reversal rejected.
- **Fallback narrative:** `cert:reversal:functional` 10/10 if inline prep fails.
- **API evidence:** `reversal/successful-reversal` PASS; execute ~3994 ms.

---

### Step 12 — RBAC enforcement
**Time:** 1 min | **Role:** Officer (`officer@wilms.demo`)

- Sign in as Officer; attempt reconciliation submit or restricted admin action.
- **Show:** HTTP 403 — "You do not have permission" (not "session expired" — 4D.1 fix).
- **API evidence:** `authentication/rbac-officer-denied` PASS, 3 ms.

---

### Step 13 — Logout & close
**Time:** 2 min | **Role:** Presenter

- Logout via UI (clears `wilms_session` cookie via `/api/auth/logout`).
- Summarize certified workflows: borrowers, reconciliation, uploads (local), reversals, RBAC.
- Open Q&A; reference `P14.3B-phase-4d2-functional-certification.md` for evidence detail.

---

## Timing Summary

| Block | Steps | Minutes |
|-------|-------|---------|
| Intro | 1 | 2 |
| Identity & borrowers | 2–4 | 4 |
| Loan & collection | 5–6 | 5 |
| Reconciliation | 7–9 | 5 |
| Upload & reversal | 10–11 | 5 |
| Security & close | 12–13 | 3 |
| **Total** | **13** | **~24** |

Add 5 minutes buffer for login switches and network latency on Neon.

---

## What Not to Demo (Out of Scope)

- Cloudinary CDN upload (not configured)
- Reconciliation history list UI (backend only)
- Loan pool administration (unless stakeholder asks — reference 4C.4 cert)
- Performance load test (reference 4C.4 batch numbers)

---

## Screenshot Capture (Demo Rehearsal)

Deferred from automated certification. During rehearsal, capture:

1. Collector reconciliation form — zero variance preview
2. Successful reconciliation submit toast/state
3. Upload preview rendering
4. Reversal confirmation screen
5. Officer 403 error message (distinct from 401)

Store under `docs/page-validation/screenshots/P14.3B-4D.2/` (create at rehearsal time).
