# P14.5C — Pull Request Description

## Summary

Production deployment architecture for full-stack WILMS (not frontend-only).

- **Recommended:** Option A — Vercel (frontend + BFF) + Railway (Express API) + Neon + Cloudinary
- Vercel investigation: 5 root causes documented (`.next` path, monorepo install, backend separation, credentials, mock env)
- Backend hosting: Railway with `apps/backend/railway.toml`
- Alternative: `render.yaml` for Option B
- CI: `.github/workflows/ci.yml` (validation on PR/main)
- Deploy workflow template (disabled until secrets configured)
- Production safety: BFF requires `WILMS_API_UPSTREAM` in production; backend binds `0.0.0.0` in production
- Environment audit: `JWT_SECRET` does not exist — uses `WILMS_SESSION_SECRET`

## Architecture

```text
Browser → Vercel (Next.js + BFF /api/wilms) → Railway (Express) → Neon + Cloudinary
```

## Test plan

- [x] `npm run type-check`
- [x] `npm run lint`
- [x] `npm run build`
- [x] `npm run test -w @wilms/api`
- [x] `npm run test`
- [ ] Deploy Railway API → `/health` 200
- [ ] Deploy Vercel frontend with `WILMS_API_UPSTREAM`
- [ ] Production smoke tests (P14.5C-smoke-test-report.md)

## Vercel settings

| Setting | Value |
|---------|-------|
| Root Directory | `apps/frontend` |
| Install | `cd ../.. && npm ci` |
| Build | `cd ../.. && npm run build -w @wilms/frontend` |

## Docs

- `P14.5C-deployment-architecture.md`
- `P14.5C-hosting-comparison.md`
- `P14.5C-vercel-investigation.md`
- `P14.5C-backend-deployment.md`
- `P14.5C-production-environment.md`
- `P14.5C-deployment-checklist.md`
- `P14.5C-final-release-report.md`
