# P14.6 — Console & Network Error Triage (STOP GATE 2 — preliminary)

**Status:** PRELIMINARY — full local + production browser audit pending  
**Date:** 2026-06-30

---

## Error Set A — 401 on `/login`

### Reported

```
Failed to load resource: the server responded with a status of 401 ()
login:1
login?next=%2F:1
```

### Investigation

| Probe | Result | Evidence |
|-------|--------|----------|
| `GET /api/wilms/auth/session` (no cookie) | **200** | PowerShell `Invoke-WebRequest` 2026-06-30 |
| `GET /api/wilms/loans` (no cookie) | **401** | Expected RBAC behavior |
| Login page components fetch on mount? | **No API calls** in `LoginForm`, `AuthHydrator`, `PermissionProvider` (`enabled: Boolean(user)`) | Code review |
| CSRF pre-fetch on login | Only on **submit** via `authService.login` | `authService.ts:16` |

### Preliminary classification

| Hypothesis | Likelihood | Notes |
|------------|------------|-------|
| **Expected 401** — stale session / prefetch of protected BFF route | Medium | Unauthenticated `GET /api/wilms/loans` returns 401 by design (`apps/frontend/src/app/api/wilms/[...path]/route.ts`) |
| **Next.js router prefetch** of `next=/` target | Medium | URL `login?next=%2F` appears in report — may correlate with middleware redirect from `/` |
| CSRF misconfiguration | Low | `GET /api/auth/csrf` returns 200 |
| App bug requiring code fix | **Unconfirmed** | Need DevTools Network tab capture: exact URL + initiator stack |

### Required follow-up (before fix approval)

1. Reproduce locally: open `/login` and `/login?next=/` with DevTools → Network → filter 401
2. Record exact failing URL, method, response body
3. Repeat in incognito on production
4. If initiator is application code → propose minimal fix at STOP GATE 2

---

## Error Set B — `runtime.lastError`

### Reported

```
Unchecked runtime.lastError: A listener indicated an asynchronous response by returning true,
but the message channel closed before a response was received
```

### Preliminary classification

| Check | Status |
|-------|--------|
| Signature matches browser extension messaging | **Likely** |
| Found in application source | **No matches** in `apps/frontend/src` |
| `PwaInstallBanner` uses `beforeinstallprompt`, not `chrome.runtime` | `PwaInstallBanner.tsx:23-26` |

### Required follow-up

1. Reproduce in **incognito with extensions disabled**
2. If absent → classify **Environment Noise**, close with no code change
3. If persists → trace service worker / extension conflict

---

## Expanded audit status

| Category | Status |
|----------|--------|
| Unhandled promise rejections | Pending browser session |
| Mixed content | Pending |
| 404/500/CORS on primary pages | Pending |
| React hydration warnings | Pending |
| Production-only errors | Pending |
| Network: localhost/mock URLs | Smoke tests cover; manual audit pending |

---

> ## STOP GATE 2
> Full audit table will be completed after browser reproduction sessions.
> **No code fixes committed** until App Bug classifications are approved.
