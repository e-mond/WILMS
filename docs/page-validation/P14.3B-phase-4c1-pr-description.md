# PR: [P14.3B-Phase-4C.1] Reconciliation Schema & Migration

**Base:** `feature/p14.3b-phase-4b-architecture`  
**Head:** `feature/p14.3b-phase-4c1-schema`

---

## Executive Summary

Implements the **database layer only** for Collector Cash Reconciliation MVP per approved 4B architecture. Adds `financial_reconciliations` and `reconciliation_history` tables with enums, constraints, and indexes.

**No repositories, services, routes, or frontend changes.**

---

## Migration Impact

| Change | Risk |
|--------|------|
| New tables `financial_reconciliations`, `reconciliation_history` | Low — additive |
| New enums | Low |
| `idempotency_scope` + `RECONCILIATION_SUBMIT` | Low — forward enum extension |
| Existing payments/loans/pools/adjustments/reversals | **Unchanged** |

Migration: `0006_financial_reconciliations.sql`

---

## Schema Decisions Implemented

| AD-ID | Implementation |
|-------|----------------|
| AD-REC-06 | `financial_reconciliations` + `reconciliation_history` |
| AD-REC-07 | Partial unique `(collector_user_id, reconciliation_date) WHERE status = 'SUBMITTED'` |
| AD-REC-02/03 | Snapshot columns for expected, system_recorded, variances |

---

## Rollback Strategy

1. Revert merge — tables unused until 4C.2 service layer
2. Manual DROP of new tables/types if needed (see certification doc)
3. No impact on existing financial records

---

## Validation Evidence

| Check | Result |
|-------|--------|
| `npm run type-check` | PASS |
| `npm run lint` | PASS |
| `npm run build` | PASS |
| `npm run test` | PASS (200/200) |
| `npm run db:migrate` | PASS |
| `npm run db:seed` | PASS |

---

## Known Limitations

- `drizzle-kit generate` failed (ESM import); migration hand-authored
- No service/API — submit flow blocked until 4C.2
- `reconciliation_variances` table deferred per architecture

---

## Approval Request

After merge, proceed to **P14.3B.4C.2 Repository & Service Layer**.

**Awaiting human review. Do not merge automatically.**

---

## Labels

`database`, `schema`, `financial`, `reconciliation`, `phase-4c`, `review-required`

## Milestone

`P14.3B Financial Controls`
