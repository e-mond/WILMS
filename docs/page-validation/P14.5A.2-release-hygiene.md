# P14.5A.2 — Release Hygiene

**Scan date:** 2026-06-27  
**Scope:** `apps/frontend`, `apps/backend`, root config

---

## TODO / FIXME markers

| Location | Count |
|----------|-------|
| `apps/frontend/src` | **0** |
| `apps/backend/src` (excl. verification) | **0** |
| Verification scripts | console output only — intentional |

No unresolved TODO/FIXME in application source.

---

## Debug logging

| Area | Finding |
|------|---------|
| Backend product code | Uses structured `logger` in `index.ts` — no raw `console.log` in modules |
| Verification harness | Extensive `console.log` — expected for cert scripts |
| Frontend | No production debug log patterns flagged |

---

## Dead code indicators

| Item | Evidence | Action |
|------|----------|--------|
| `db/schema/expenses.ts` | No `modules/expenses` routes | Orphan schema — V2 |
| `db/schema/notifications.ts` | No notification routes | Orphan schema — V2 |
| `db/persistence.ts` in-memory store | Used when no DATABASE_URL | Keep for dev |
| Mock service tree | `services/mock/*` | Required for demo mode — not dead |
| 20+ stale git branches | `git branch -a` | Housekeeping optional |

---

## Scripts (`apps/backend/package.json`)

| Script category | Count | Notes |
|-----------------|-------|-------|
| Verification/cert | ~25 | All referenced in README or phase docs |
| db:migrate, db:seed | Active | Required |
| dev, start, test | Active | Required |
| `verify:pools`, `verify:adjustments` | Active | Phase cert — still valid |

No unused npm scripts identified in backend package.json.

---

## Packages

Monorepo workspaces: `@wilms/frontend`, `@wilms/api`, `packages/shared-*`.

| Check | Result |
|-------|--------|
| Duplicate express versions | Single lockfile — not audited deeply |
| Unused dependencies | Not run `depcheck` — manual sample: all major deps referenced |
| Turbo | `turbo.json` present — build orchestration |

---

## Environment variables

### Documented in `.env.example` (root)

79 lines — covers frontend, backend, uploads, mail, SMS, E2E, P14.5A hardening, cert thresholds.

### Backend-specific examples

- `apps/backend/.env.example`
- `apps/backend/.env.production.example`
- `apps/backend/.env.local.example`

### Potentially unused / infrastructure-only

| Variable | Used | Notes |
|----------|------|-------|
| `MAIL_*`, `RESEND_*` | Config only | No workflow sends mail |
| `SMS_*`, `ARKESEL_*`, `TWILIO_*` | Config only | No workflow sends SMS |
| `WILMS_GIT_COMMIT` | Health report | Set at deploy |
| `WILMS_VERIFY_*_P95_MS` | Cert harness | Optional |

### Local `.env` drift

- `NEXT_PUBLIC_API_BASE_URL=http://localhost:3000/` — BFF proxy path (correct for Next proxy)
- `UPLOAD_PROVIDER=cloudinary` — configured (user statement confirmed)
- `NODE_ENV=development` — forces mock mode unless `NEXT_PUBLIC_USE_MOCK=false`

---

## Documentation hygiene

| Issue | Count/impact |
|-------|--------------|
| Phase docs under `docs/page-validation/` | 220+ files |
| Superseded claims in README | High confusion risk |
| Multiple executive summaries (P14.3C, P14.4, P14.5A, P14.5A.1) | Need consolidation post-freeze |

---

## Git working tree

```
## feature/p14.5a-production-hardening...origin/feature/p14.5a-production-hardening
?? "(env): update example environment configuration"  # stray untracked file
```

Recommend delete or commit stray env artifact before merge.

---

## Hygiene verdict

Application source is **clean of TODO noise**. Primary hygiene debt is **documentation staleness** and **orphan DB tables**, not runtime dead code in financial core.

No hygiene fixes applied in this audit.
