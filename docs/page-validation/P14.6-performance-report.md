# P14.6 — Performance Report

**Release:** v0.2.2  
**Branch:** `release/p14.6-cd-sync-ux`  
**Date:** 2026-06-30

---

## Bundle Budgets (CI-enforced)

Command: `npm run bundle:budget-check` (after `npm run build`)

| Metric | Measured | Budget | Result |
|--------|----------|--------|--------|
| JS total (gzip, first-load chunks) | **168.5 KB** | 350 KB | **PASS** |
| CSS total (gzip) | **8.1 KB** | 100 KB | **PASS** |
| Chunks > 200 KB gzip | none | warn threshold | **PASS** |

Script: `scripts/bundle-budget-check.mjs`  
CI step: `.github/workflows/ci.yml` → `Bundle budget check`

---

## First Load JS (Next.js build)

| Metric | Value |
|--------|-------|
| Shared First Load JS | **87.8 kB** |
| Routes | 43 |
| Middleware | 30 kB |
| Largest route | `/approver/pending/[id]` — ~591 kB (lazy PDF/export deps) |

First-load chunk filter excludes per-route lazy chunks; shared baseline unchanged from v0.2.1.

---

## Performance Budget Script

Command: `npm run perf:budget-check`  
Delegates to bundle budgets; Lighthouse runs deferred to staging deploy pipeline.

---

## v0.2.2 Impact

| Change | Impact |
|--------|--------|
| Sync API routes | Server-only — no frontend bundle change |
| `loading.tsx` (collector, approver) | Negligible — shared `LoadingSpinner` |
| Offline queue toast hook | Small client addition |
| CI budget gates | Build-time only |

---

## Verdict

**PASS** — 168.5 KB gzip JS and 87.8 KB shared first load within budgets. No regression from v0.2.1 baseline.
