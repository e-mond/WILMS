# P14.5F Versioning Report

**Date:** 2026-06-29

---

## Source of Truth

**File:** `package.json` at monorepo root  
**Field:** `"version": "0.1.0"` (line 3)

---

## Implementation

| File | Change |
|------|--------|
| `apps/frontend/next.config.mjs` | Reads root `package.json`; sets `env.NEXT_PUBLIC_APP_VERSION` at build time |
| `apps/frontend/src/components/layout/AppVersionBadge.tsx` | Renders `v{version}` from `NEXT_PUBLIC_APP_VERSION` |
| `apps/frontend/src/app/(auth)/login/page.tsx` | Footer badge below login form |

No hardcoded version string in components.

---

## Update Procedure (one line)

Bump `"version"` in root `package.json`, then redeploy frontend (`vercel deploy --prod` from repo root).

---

## Production Verification

**Status:** Pending Vercel redeploy after `next.config.mjs` + badge changes.

**Local build check:** Run `npm run build -w @wilms/frontend` and inspect login page HTML for `v0.1.0`.
