# P14.5F Security Review

**Date:** 2026-06-29  
**Auditor:** Automated + command evidence  
**Production URLs:** Frontend `https://wilms.vercel.app` · API `https://wilms-production.up.railway.app`

---

## Transport Security

| Check | Current | Expected | Result | Action |
|-------|---------|----------|--------|--------|
| HTTPS enforced (frontend) | `curl -sI https://wilms.vercel.app/login` → HTTP/1.1 **200** over TLS | TLS only | **PASS** | — |
| HSTS (frontend) | `Strict-Transport-Security: max-age=63072000; includeSubDomains; preload` | Present | **PASS** | — |
| HTTPS enforced (backend) | `curl -sI https://wilms-production.up.railway.app/health` → **200** over TLS | TLS only | **PASS** | — |
| HSTS (backend) | `strict-transport-security: max-age=31536000; includeSubDomains` | Present | **PASS** | — |

---

## Session and Cookies

| Check | Current | Expected | Result | Action |
|-------|---------|----------|--------|--------|
| HttpOnly | Smoke `session-cookie-httponly` **PASS** on `wilms.vercel.app` | HttpOnly | **PASS** | — |
| Secure | `apps/frontend/src/lib/auth/cookies.ts:16` — `secure: NODE_ENV === 'production'` | Secure in prod | **PASS** | — |
| SameSite | `cookies.ts:17` — `sameSite: 'lax'` | Lax or Strict | **PASS** | — |
| Session secret length | Railway `WILMS_SESSION_SECRET` — **64 hex chars** (≥32) | ≥32 random | **PASS** | — |
| Session expiry | `env.ts:36` — `sessionDurationMs: 24h`; token includes `expiresAt` | Enforced | **PASS** | — |

---

## API Security

| Check | Current | Expected | Result | Action |
|-------|---------|----------|--------|--------|
| Helmet active | `apps/backend/src/http/app.ts:42-47` | Active | **PASS** | — |
| X-Frame-Options | Response: `x-frame-options: SAMEORIGIN` | Set | **PASS** | — |
| X-Content-Type-Options | Response: `x-content-type-options: nosniff` | Set | **PASS** | — |
| Referrer-Policy | Response: `referrer-policy: no-referrer` | Set | **PASS** | — |
| CORS origin | Response: `access-control-allow-origin: https://wilms.vercel.app` | Production domain only | **PASS** | Updated Railway `WILMS_CORS_ORIGIN` 2026-06-29 |
| CORS credentials | `app.ts:52` — `credentials: true` | true | **PASS** | — |
| CSP | Response includes `content-security-policy: default-src 'self';...` | Present | **PASS** | Policy documented below |
| X-Powered-By (API) | Not present; `Server: railway-hikari` | Suppressed | **PASS** | — |
| X-Powered-By (frontend) | Was `X-Powered-By: Next.js` | Suppressed | **FAIL → FIX** | Added `poweredByHeader: false` in `next.config.mjs` (pending redeploy) |

**CSP policy (API, Helmet default + production):**
`default-src 'self'; base-uri 'self'; font-src 'self' https: data:; form-action 'self'; frame-ancestors 'self'; img-src 'self' data:; object-src 'none'; script-src 'self'; script-src-attr 'none'; style-src 'self' https: 'unsafe-inline'; upgrade-insecure-requests`

---

## Upload Restrictions

| Check | Current | Expected | Result | Action |
|-------|---------|----------|--------|--------|
| MIME allowlist (server) | `validation.ts:15` — `config.allowedMimeTypes.includes(input.mimeType)` | Server-side | **PASS** | — |
| Size limit (server) | `validation.ts` + `config.ts:31` — default 10MB | Server-side | **PASS** | — |
| Cloudinary storage | Health: `activeProvider: cloudinary` | Not local disk | **PASS** | — |

---

## Environment Variable Security

| Check | Current | Expected | Result | Action |
|-------|---------|----------|--------|--------|
| Secrets in committed `.env` | `.env` in `.gitignore` (line 2) | Not committed | **PASS** | — |
| Cloudinary secret in logs | `cert-upload-environment.ts` masks with `mask()` | Not logged | **PASS** | — |
| `NEXT_PUBLIC_USE_MOCK` | Vercel production env set to `false` | false/absent | **PASS** | — |
| `NODE_ENV` | Railway: `production` | production | **PASS** | — |
| Hardcoded credentials in source | Demo passwords in `demo-users.ts`, cert scripts, e2e helpers — **not production secrets**; production uses hashed DB passwords | No prod creds in source | **PASS** (with note) | Demo users still in **production DB** — Objective 3 / STOP GATE 2 |

---

## Summary

- **Pass:** 22/23 checks
- **Fail (fixed locally, pending deploy):** Frontend `X-Powered-By` — `poweredByHeader: false` added
- **Open risk:** Demo credentials in production database (STOP GATE 2)
- **Open risk:** CSRF not implemented (STOP GATE 3)
