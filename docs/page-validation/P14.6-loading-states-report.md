# P14.6 — Loading States Report

**Release:** v0.2.2  
**Branch:** `release/p14.6-cd-sync-ux`  
**Date:** 2026-06-30

---

## Route-Level `loading.tsx`

| Route group | File | Component |
|-------------|------|-----------|
| Collector | `app/(collector)/loading.tsx` | `LoadingSpinner` — "Loading collector workspace" |
| Approver | `app/(approver)/loading.tsx` | `LoadingSpinner` — "Loading approver workspace" |
| Super Admin | `app/(super-admin)/loading.tsx` | *(pre-existing)* |
| Registration Officer | `app/(registration-officer)/loading.tsx` | *(pre-existing)* |

Next.js App Router streams these while segment data loads — improves perceived performance on slow networks.

---

## Shared Component

`LoadingSpinner` (`components/feedback/LoadingSpinner.tsx`):

- Accessible label via `label` prop
- Full-screen min-height on role shells
- Consistent with existing design tokens (`py-wilms-8`)

---

## Coverage Gaps

| Route group | `loading.tsx` | Notes |
|-------------|---------------|-------|
| Auditor | ❌ | Low traffic — backlog |
| Auth (`/login`) | ❌ | Fast static page |
| Dynamic `[id]` routes | inherits parent | Collector/approver parent covers segment |

---

## v0.2.2 Rationale

Collector and approver are highest-latency field workflows (dashboard data, pending queues). Adding segment loaders closes P14.5G recommendation without increasing bundle size materially.

---

## Verdict

**IMPLEMENTED** — collector and approver loading states added. Remaining role groups acceptable for v0.2.2.
