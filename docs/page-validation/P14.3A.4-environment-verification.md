# P14.3A.4 — Environment Verification

**Phase:** P14.3A.4 Phase 1  
**Date:** 2026-06-09  
**Status:** Complete (evidence-based)

---

## Scope

Confirm the live stack can be started with Neon-backed configuration: env loading, migrations, backend startup, and frontend production mode with BFF upstream.

---

## Environment Sources

| Source | Path | Loaded by |
|--------|------|-----------|
| Monorepo secrets | `.env` (gitignored) | `apps/backend/src/config/load-env.ts` |
| Backend examples | `apps/backend/.env*.example` | Documentation |
| Frontend examples | `apps/frontend/.env*.example` | Documentation |
| Root example | `.env.example` | Documentation |

**Verified:** Backend startup log shows dotenv injection from monorepo root:

```text
◇ injected env (10) from ..\..\.env
WILMS API listening on http://127.0.0.1:4000 [postgresql (Neon)]
Upload provider: local | Mail: none | SMS: none
```

---

## DATABASE_URL / Neon

| Check | Command | Result |
|-------|---------|--------|
| `DATABASE_URL` present | `verify:live` startup | `configured` |
| Migrations | `npm run db:migrate -w @wilms/api` | ✅ `migrations applied successfully!` |
| Runtime driver | Backend log | `[postgresql (Neon)]` |

**Note:** `drizzle.config.ts` previously imported `./src/config/load-env.js`, which failed under `drizzle-kit` (no compiled `.js`). Fixed in P14.3A.4 by loading monorepo `.env` directly in `drizzle.config.ts`.

---

## Live Stack Ports (this session)

| Service | Host:Port | Env |
|---------|-----------|-----|
| Backend API | `127.0.0.1:4000` | Default `WILMS_API_PORT` |
| Frontend (prod smoke) | `127.0.0.1:3010` | `PORT=3010`, `NODE_ENV=production` |
| E2E Playwright | `127.0.0.1:3011` | `CI=1`, `PLAYWRIGHT_PORT=3011` |

---

## Frontend Live Mode Requirements

From `apps/frontend/src/data-provider/types.ts` — API mode requires **all** of:

1. `NEXT_PUBLIC_API_BASE_URL` set (BFF URL, e.g. `http://127.0.0.1:3010/api/wilms`)
2. `NODE_ENV=production`
3. Demo overrides **not** set (`NEXT_PUBLIC_DEMO_MODE`, `NEXT_PUBLIC_FORCE_DEMO_MODE`, `NEXT_PUBLIC_API_DISABLED`)

BFF upstream for proxy:

```env
WILMS_API_UPSTREAM=http://127.0.0.1:4000
```

**Local `.env` note:** User `.env` has `NEXT_PUBLIC_API_BASE_URL=http://localhost:3000/` (root, not BFF path). Production live smoke used explicit overrides on the command line.

---

## Validation Gates (2026-06-09 session)

| Gate | Result |
|------|--------|
| `npm run type-check` | ✅ Pass |
| `npm run lint` | ✅ Pass |
| `npm run build` | ✅ Pass (42 routes) |
| `npm run test` | ✅ Pass (400/400 — 200+200 shards) |
| `npm run db:migrate -w @wilms/api` | ✅ Pass (after drizzle config fix) |
| `CI=1 PLAYWRIGHT_PORT=3011 npm run test:e2e` | ✅ 185 passed, 1 skipped |
| `npm run verify:financial -w @wilms/api` | ⚠️ **58/60** (see production certification) |
| `npm run verify:live -w @wilms/api` | ✅ **23/23** |

---

## Blockers / Gaps

| Item | Detail |
|------|--------|
| Financial harness green | **Not met** — 2 Neon WAN latency checks fail |
| Default dev `.env` BFF URL | Trailing slash + missing `/api/wilms` path |
| Mail / SMS / Cloudinary | Infrastructure present; providers `none` in this session |

---

## P14.3A.4 Environment Verdict

**Live environment operational** for backend + Neon + production frontend BFF smoke.  
**Not fully certified** until financial harness meets targets or targets are formally adjusted for remote Neon.
