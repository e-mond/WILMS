# P14.3A.2 — Runtime Validation

**Phase:** P14.3A.2 Phase 5  
**Date:** 2026-06-09  
**Status:** Complete

---

## Validation Commands

| Command | Result | Evidence |
|---------|--------|----------|
| `npm run type-check` | ✅ Pass | Frontend + `@wilms/api` tsc clean |
| `npm run lint` | ✅ Pass | `next lint` — no warnings/errors |
| `npm run build` | ✅ Pass | 42 routes, exit 0 (~67s) |
| `npm run test` | ✅ Pass | 400 tests (200+200 shards), exit 0 (~578s) |
| `CI=1 npm run test:e2e` | ❌ **Not completed** | Port conflict — see below |
| `npm run db:migrate -w @wilms/api` | ✅ Pass | With `DATABASE_URL` from `.env.example` |
| `npm run db:seed -w @wilms/api` | ✅ Pass (after SEED-001 fix) | RBAC + financial core |
| `npm run verify:financial -w @wilms/api` | ⚠️ Partial | 54/59 passed (concurrency + Neon latency) |

---

## E2E Failure (this session)

```
Error: http://127.0.0.1:3001/login is already used,
make sure that nothing is running on the port/url
or set reuseExistingServer:true in config.webServer.
```

**Cause:** Frontend dev server started on `:3001` before E2E; Playwright webServer could not bind.

**Prior evidence (P14.3A.1):** 185 passed, 1 skipped — mock-backed suite.

---

## Dev Server — Backend

| Check | Result |
|-------|--------|
| `npm run dev:api` with `DATABASE_URL` | ❌ `EADDRINUSE` — port 4000 already in use |
| Existing process on `:4000` | ✅ Responding |
| `GET /health` | ✅ 200 `{"data":{"status":"ok","service":"wilms-api"}}` |
| `GET /loans` (no auth) | ✅ 401 |
| Persistence mode of existing process | **Unknown** — likely in-memory (no `.env` auto-load) |

Attempted startup log (new process):

```
Error: listen EADDRINUSE: address already in use 127.0.0.1:4000
```

---

## Dev Server — Frontend

| Check | Result |
|-------|--------|
| `npm run dev -w @wilms/frontend` | ✅ Started |
| Port | `:3001` (3000 in use) |
| Ready | ✅ `✓ Ready` after compile |
| URL | `http://localhost:3001` |
| Demo mode | Active (no `NEXT_PUBLIC_API_BASE_URL` in process) |

Startup output:

```text
⚠ Port 3000 is in use, trying 3001 instead.
▲ Next.js 14.2.18
- Local: http://localhost:3001
✓ Starting...
✓ Ready
```

---

## Missing Environment Variables (default dev)

| Variable | Observed behavior |
|----------|-------------------|
| `DATABASE_URL` | Not in process → backend in-memory |
| `NEXT_PUBLIC_API_BASE_URL` | Empty → mock services |
| `WILMS_API_UPSTREAM` | Defaults OK for BFF |
| `.env` file | Absent |

---

## Dependency Failures

None observed for type-check, lint, build, or unit tests.

---

## Route Failures

No route failures during build. Runtime backend auth correctly rejects unauthenticated loan access.

---

## Financial Verification Detail

With `DATABASE_URL` configured:

| Section | Passed |
|---------|--------|
| Unit | 12/12 |
| Security / RBAC | 6/6 |
| Database | 0 — process exit on `NOT_FOUND` in performance checks |

---

## Runtime Validation Summary

| Component | Starts | Errors |
|-----------|--------|--------|
| Frontend dev | ✅ Yes | Port fallback to 3001 |
| Backend dev | ⚠️ Blocked | Port 4000 occupied; existing instance healthy |
| Neon migrate | ✅ Yes | — |
| Neon seed | ❌ No | UUID seed defect |
| Full test suite | ✅ Yes | — |
| E2E | ❌ Blocked this session | Port conflict |
