# P13.3 E2E Failure Classification

**Date:** 2026-06-09  
**Baseline:** `P13.2-e2e-run-v4-r2.log` — 134 passed, **11 failed**, 10 flaky  
**Final:** `P13.3-e2e-run-r2.log` — **185 passed, 0 failed**, 0 flaky, 1 skipped (16.5m)

## Classification of the 11 P13.2 Failures

| # | Spec | Project | Root cause (code evidence) | P13.3 fix |
|---|------|---------|---------------------------|-----------|
| 1–3 | `accessibility.spec.ts` super admin dashboard/groups/settings | desktop | `color-contrast` on live badge (`AppNavbar.tsx` pulse on full span); `scrollable-region-focusable` on `DataTable` wrapper; `nested-interactive` on `<tr role="button">` with cell `<Link>`s in `GroupsManagementPanel.tsx` | Badge dot pattern + `data-live-badge`; `DataTable` scroll `role="region"` + removed row button semantics; settings gold buttons `text-white` |
| 4–7 | `accessibility.spec.ts` collector dashboard, approver pending, groups, settings | mobile | `aria-required-parent` — `ShellNavLink` `tabMode` `role="tab"` without `tablist` parent (`OperationalMobileNavigation.tsx`); collector inactive tab `text-text-tertiary` contrast | `ul role="tablist"`, `li role="presentation"`; inactive tabs `text-text-muted` |
| 8–9 | `app-lock.spec.ts` | desktop + mobile | Test expected `'Six-digit app lock enabled.'` / `'App locked'`; product uses `'Enabled on this device'` / `'Welcome back'` (`AppLockSetupPanel.tsx`, `AppLockOverlay.tsx`) | E2E aligned to product copy; idle lock via `__WILMS_E2E_APP_LOCK_IDLE_MS` init script |
| 10–11 | `shell-navbar.spec.ts:62` | desktop + mobile | Strict mode: two "Online" nodes — live badge + `ConnectionStatusChip` in `ShellNavbarActions.tsx` | Assert `[data-live-badge="true"]` only |

## Additional Failures Found During P13.3 (r1 → r2)

| Spec | Cause | Fix |
|------|-------|-----|
| Desktop axe (8 scans) | Live badge `text-white` on `bg-status-active` failed when `animate-pulse` lightened computed background | Pulse moved to inner dot only (`AppNavbar.tsx`) |
| Desktop loan pools axe | `#app-aside` `overflow-y-auto` without keyboard access (`AppAside.tsx`) | `tabIndex={0}` on aside |
| Flaky responsive / shell / logout / workflows | Global `NEXT_PUBLIC_APP_LOCK_IDLE_MS=2000` locked UI mid-suite (`playwright.config.ts`, `AppLockHandler.tsx`) | Default E2E idle 3600000ms; app-lock spec overrides via `addInitScript` |

## Validation Evidence

```
CI=1 npm run test:e2e → P13.3-e2e-run-r2.log
185 passed | 0 failed | 0 flaky | 1 skipped
```

Skipped: `shell-navbar.spec.ts` collector connection chip (intentional `test.skip` on mobile project, line 207).
