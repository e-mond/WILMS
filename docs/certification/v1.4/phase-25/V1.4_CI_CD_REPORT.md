# V1.4 CI/CD Report

**Date:** 18 July 2026  
**Workflow:** `.github/workflows/ci.yml`  
**Trigger:** Push to `main`, `release/**`; PRs to `main`

---

## Summary

Phase 25 extends the CI `validate` job with three new gates: Node 22 consistency, migration journal integrity, and version consistency. Existing gates (type-check, lint, build, tests, security) are unchanged.

---

## Validate job — full step order

| Step | Command | Phase 25 change |
|------|---------|-----------------|
| Checkout | `actions/checkout@v4` | — |
| Setup Node | `node-version: '22'` | Pre-existing |
| Install | `npm ci` | — |
| **Node 22 consistency** | `npm run verify:node` | **NEW** |
| Type check | `npm run type-check` | — |
| Lint | `npm run lint` | — |
| **Migration journal integrity** | `npm run verify:migrations` | **NEW** (script existed; now in CI) |
| **Version consistency** | `npm run verify:version` | **NEW** (script existed; now in CI) |
| API integrity | `npm run verify:api-integrity` | — |
| API coverage | `npm run verify:api-coverage` | — |
| Mock import guard | `npm run verify:mock-guard` | — |
| Build frontend | `npm run build` | — |
| Bundle budget | `npm run bundle:budget-check` | — |
| Performance budget | `npm run perf:budget-check` | — |
| Backend tests | `npm run test -w @wilms/api` | Now 158 tests |
| Frontend tests | `npm run test` | — |

---

## Security job (unchanged)

| Step | Detail |
|------|--------|
| `npm audit --audit-level=critical` | Critical-only gate |
| `gitleaks/gitleaks-action@v2` | Secret scan |

Node 22 also used in security job.

---

## New scripts (root `package.json`)

| Script | Implementation |
|--------|----------------|
| `verify:node` | `scripts/verify-node-version.mjs` |
| `verify:migrations` | `npm run verify:migrations -w @wilms/api` |
| `verify:version` | `scripts/verify-version-consistency.mjs` |
| `drill:backup-restore` | `scripts/backup-restore-drill.mjs` (not in CI — operator-only) |

---

## Local verification (mirrors CI)

From [`evidence/local-gates.txt`](./evidence/local-gates.txt):

```
type-check: PASS
api tests: 158/158 PASS (47 files)
verify:node: PASS
verify:migrations: PASS (29 journal entries through 0028)
verify:version: PASS 1.4.0
drill:backup-restore: SKIPPED (no DB URLs — expected)
```

---

## Version gate details

`verify:version` checks:

- Root, frontend, backend `package.json` → **1.4.0**
- `CHANGELOG.md` contains `[1.4.0]`
- `/health` version (when API runnable)
- Login page label (when frontend build checked)

---

## Migration gate details

`verify:migrations` confirms:

- 29 journal entries, sequential integrity
- Latest: `0028_v140_platform_foundation`
- Database apply check skipped when `DATABASE_URL` unset (CI passes journal-only)

---

## Verified

| Item | Status |
|------|--------|
| CI YAML includes new steps | ✓ |
| `verify:node` passes locally | ✓ |
| `verify:migrations` passes (29 entries) | ✓ |
| `verify:version` passes (1.4.0) | ✓ |
| Backend tests in CI path | 158 tests |
| Concurrency group prevents duplicate runs | ✓ |

---

## Blocked / not in CI

| Item | Status | Rationale |
|------|--------|-----------|
| `drill:backup-restore` | Not in CI | Requires secrets + Neon branches |
| Redis integration test | Not in CI | No Redis service container |
| E2E Playwright | Separate script | Not part of validate job |
| Production deploy workflow | Not in repo | Railway/Vercel operator-managed |
| `verify:deploy-sync` | Not in CI | Post-deploy check |

---

## Deploy implications

Merging v1.4.0 to `main` will run all gates on push. Operator must:

1. Apply migration `0028` on Neon before/at deploy
2. Set `REDIS_URL` if durable queues desired
3. Run `drill:backup-restore` outside CI

---

## Conclusion

CI/CD gates for Phase 25 are **implemented and locally verified**. CI run on GitHub Actions remote was not captured in this evidence folder — local mirror passes all validate-equivalent checks.
