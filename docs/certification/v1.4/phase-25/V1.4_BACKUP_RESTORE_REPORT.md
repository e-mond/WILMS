# V1.4 Backup & Restore Report

**Date:** 18 July 2026  
**Script:** `npm run drill:backup-restore` → `scripts/backup-restore-drill.mjs`  
**Status:** **SKIPPED** — no database URLs provided

---

## Summary

Phase 25 adds an automated backup/restore drill script that produces evidence JSON artifacts. The certification run **did not execute a restore** — credentials were absent by design. **No restore success is claimed.**

---

## Drill script behavior

| Condition | Exit | Evidence |
|-----------|------|----------|
| Missing `WILMS_BACKUP_DATABASE_URL` or `WILMS_RESTORE_DATABASE_URL` | 0 (SKIPPED) | JSON with `status: "SKIPPED"` |
| Restore target looks like production (`prod` / `production` in URL) | 2 (REFUSED) | No evidence write |
| Backup URL === restore URL | 2 (REFUSED) | No evidence write |
| `pg_dump` + `pg_restore` succeed | 0 (PASSED) | JSON with `rtoSeconds`, dump path |
| Command failure | 1 (FAILED) | JSON with error message |

**Safety:** Script refuses production-looking restore targets. Never fabricates PASSED status.

---

## Certification evidence

**File:** [`evidence/backup-restore-drill-2026-07-18T09-51-45-068Z.json`](./evidence/backup-restore-drill-2026-07-18T09-51-45-068Z.json)

```json
{
  "status": "SKIPPED",
  "reason": "WILMS_BACKUP_DATABASE_URL and WILMS_RESTORE_DATABASE_URL required",
  "timestamp": "2026-07-18T09:51:45.069Z",
  "note": "Do not fabricate restore evidence. Provide isolated Neon branch URLs to run the drill."
}
```

**Local gates summary:** [`evidence/local-gates.txt`](./evidence/local-gates.txt) — `drill:backup-restore: SKIPPED (no DB URLs — expected)`

---

## Required environment (for PASSED run)

```bash
WILMS_BACKUP_DATABASE_URL='postgresql://...' \
WILMS_RESTORE_DATABASE_URL='postgresql://...' \
  npm run drill:backup-restore
```

**Operator requirements:**

1. Source: production snapshot or PITR branch (read-only acceptable for dump)
2. Target: **isolated** Neon branch — never production
3. `pg_dump` and `pg_restore` available on PATH
4. Evidence written to `docs/certification/v1.4/phase-25/evidence/`

---

## Production backup posture (unchanged)

Ops status reports backups as:

| Field | Value |
|-------|-------|
| `backups.status` | `external_managed` |
| `backups.provider` | `neon` |
| `backups.detail` | PITR managed in Neon; restore test in ops calendar |

v1.3.8 production cutover documented Neon PITR — Phase 25 does not supersede that evidence.

---

## Verified

| Item | Status |
|------|--------|
| Drill script exists and is invocable | ✓ |
| SKIPPED path writes honest evidence | ✓ |
| Production URL guard | ✓ (code review) |
| Script exits 0 on SKIPPED (CI-friendly) | ✓ |
| Evidence directory created | ✓ |

---

## Blocked / not verified

| Item | Status | Notes |
|------|--------|-------|
| Neon restore drill PASSED | **SKIPPED** | No `WILMS_*_DATABASE_URL` in cert environment |
| RTO / RPO measurement | **Not measured** | Requires successful drill |
| Quarterly ops calendar entry | **Not done** | Operator process |
| Application smoke post-restore | **Not done** | Requires drill + app config |

---

## Operator next steps

1. Create isolated Neon branch for restore target.
2. Run drill with both URLs set.
3. Archive PASSED evidence JSON in this folder.
4. Run `verify:migrations` + smoke against restored DB.
5. Record RTO in operations calendar.

---

## Conclusion

Backup/restore **tooling** is delivered; **restore drill is not certified** in Phase 25. Merge approval does not require PASSED restore — but **production v1.4.0 deploy should not be declared fully ops-ready** until operator completes the drill.
