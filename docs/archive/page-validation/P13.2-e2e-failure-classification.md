# P13.2 E2E Failure Classification

**Source log:** `P13.1-e2e-run-v3.log` (baseline before P13.2 fixes)  
**Baseline:** 109 passed, **42 failed**, 1 skipped, 4 flaky (156 total)  
**Date:** 2026-06-15

## Summary Matrix

| Category | Count | Root Cause | Fix Required |
| -------- | ----- | ---------- | ------------ |
| Accessibility (axe) | 14 | Verified WCAG violations: nested Settings `Link`+`Button` (`button-name`), Online badge contrast on `AppNavbar`, `DataTable` rows using `aria-selected` on `role="button"` (`aria-allowed-attr`). Collector axe test also used stale copy (`Today's borrowers`). | Fix components (`ShellNavbarActions`, `AppNavbar`, `DataTable`); align axe ready selectors in `accessibility.spec.ts`. |
| Responsive / mobile | 4 | Registration Officer and Approver shells use **operational bottom navigation** (`operationalMobileNav`), not sidebar drawer. Tests expected `Open navigation menu` from `e2e/helpers/shell.ts`. | Extend shell helper with `operationalMobileNav` path (bottom `role="tab"` nav). |
| Navigation | 14 | **Duplicate overlay panels**: `GlobalSearchPanel` and `NotificationInboxPanel` mounted in both desktop `AppNavbar` and CSS-hidden `OfficeShellMobileBar`, causing strict-mode duplicate `dialog` failures in `shell-navbar.spec.ts` and `p13-workflows.spec.ts`. | Single `ShellOverlayPanels` mount in `DashboardShell`. |
| Workflow logic | 7 | E2E expectations diverged from UI: expense page title `Record Expense` (not `Field Expenses`), settings nav button `User Management` (not `Users`), dashboard live badge text `Online` (not `LIVE`), approver mobile nav uses `role="tab"` (not `link`). | Update `p13-workflows.spec.ts`, `shell-navbar.spec.ts`, `role-journeys.spec.ts`. |
| Timing / race conditions | 3 | Intermittent `signIn` `waitForURL` timeout on collector axe (desktop retry #1); `page.goto` abort on mobile collectors shell test under parallel load. | Overlay dedupe + stable page-ready assertions before navigation. |
| Test instability / flakiness | 4 (flaky, not failed) | Same timing/selector issues: super-admin responsive @375px, groups workflow click, context drawer on mobile alerts, laptop sidebar collapse. | Deterministic waits; deduped dialogs; `.first()` on ambiguous KPI labels. |
| Permissions / RBAC | 0 | No RBAC-related failures in v3 log. | None. |
| Uploads | 0 | No upload E2E failures in v3 log. | None. |
| Forms | 0 | Category label failures were stale heading text, not form defects. | Covered under Workflow logic. |
| Other | 3 | `app-lock.spec.ts`: mandatory PIN setup in `signIn()` hides `Set up app lock` button (2). `logout.spec.ts` mobile: `Log out` inside closed profile menu (1). | `skipMandatoryAppLock` option; open profile menu before logout on mobile. |

**Total classified failures:** 42 (14 + 4 + 14 + 7 + 3 = 42; flaky items tracked separately)

## Failure Inventory (v3)

### Accessibility — 14

| Project | Test |
| ------- | ---- |
| desktop | collector dashboard, approver pending, super admin dashboard, collectors, groups, loan pools, risk flags, settings |
| mobile | collector dashboard, approver pending, super admin dashboard, groups, risk flags |

Login axe scan **passed** in v3.

### Shell / navigation — 14

| Project | Test |
| ------- | ---- |
| desktop + mobile | global search, notification inbox, breadcrumbs/LIVE badge, collectors mobile aside, loan pools mobile aside, risk flags mobile aside, risk flags laptop aside |

### Responsive — 4

| Project | Role | Test |
| ------- | ---- | ---- |
| desktop + mobile | registration-officer, approver | `@ mobile-375 office shell matches breakpoint` |

### Workflows — 7

| Spec | Tests |
| ---- | ----- |
| `p13-workflows.spec.ts` | expense form, user management settings, global search; mobile: approver pending review |

### Other — 3

| Spec | Tests |
| ---- | ----- |
| `app-lock.spec.ts` | collector enable PIN (desktop + mobile) |
| `logout.spec.ts` | collector logout (mobile) |

### Flaky — 4

| Project | Test |
| ------- | ---- |
| desktop | super admin groups directory; super-admin @ mobile-375; context drawer alerts |
| mobile | laptop collapsible sidebar |

## P13.2 Remediation Applied

| Category | Code / test change |
| -------- | ------------------- |
| Accessibility | `ShellNavbarActions` Settings link; `AppNavbar` Online badge contrast; `DataTable` `aria-pressed`; E2E ready markers |
| Navigation | `ShellOverlayPanels.tsx` + single mount in `DashboardShell` |
| Responsive | `expectOfficeShellAtBreakpoint` operational bottom-nav branch |
| Workflow / Other | E2E selector updates across `accessibility`, `shell-navbar`, `p13-workflows`, `role-journeys`, `logout`, `app-lock` specs |

## Post-fix validation

See **`P13.2-e2e-run-v4-r2.log`** and **`P13.2-production-readiness-v4.md`** for executed `CI=1 npm run test:e2e` results.
