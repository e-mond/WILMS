# P13.3 Responsive Certification Audit

**Date:** 2026-06-09  
**Certification breakpoints:** 320px, 360px, 390px, 414px, 768px (+ 1280px, 1536px regression)  
**Evidence:** `responsive-breakpoints.spec.ts`, `shell-navbar.spec.ts`, `P13.3-e2e-run-r2.log`

## Breakpoint Matrix (automated)

| Breakpoint | Spec label | Tests per run | Mobile nav expected |
|------------|--------------|---------------|---------------------|
| 320×568 | `mobile-320` | login, collector nav, 3 office shells | bottom tabs / drawer per role |
| 360×740 | `mobile-360` | same | same |
| 390×844 | `mobile-390` | same | same |
| 414×896 | `mobile-414` | same | same |
| 768×1024 | `tablet-768` | same | sidebar links |
| 1280×720 | `desktop-1280` | same | sidebar |
| 1536×900 | `wide-1536` | same | sidebar + `#app-aside` |

**Code:** `e2e/responsive-breakpoints.spec.ts` L6–14 (expanded from P13.2 `mobile-375` only).

## Role Shell Behavior (unchanged architecture)

| Role | <768px | ≥768px | Source |
|------|--------|--------|--------|
| Collector | `OperationalBottomNavigation`, no drawer | Sidebar | `CollectorShell.tsx` |
| Registration Officer / Approver | Bottom tabs (`operationalMobileNav`) | Sidebar + drawer disabled | `OfficeShell.tsx` |
| Super Admin | Mobile sidebar drawer + context FAB | Persistent sidebar + `AppAside` at xl | `DashboardShell.tsx` |

## P13.3 Stability Fixes Affecting Responsive Tests

| Issue | Fix |
|-------|-----|
| App lock overlay intercepting nav clicks at 320px (idle 2s globally) | `playwright.config.ts` idle 3600000ms; app-lock spec only sets `__WILMS_E2E_APP_LOCK_IDLE_MS` |
| Collectors mobile shell timeout | `page.goto(..., { waitUntil: 'domcontentloaded' })` in `shell-navbar.spec.ts` |

## Validation

All `responsive-breakpoints.spec.ts` and `shell-navbar.spec.ts` cases **pass** in `P13.3-e2e-run-r2.log`.

## Incomplete Work

- No physical device lab (iOS Safari, Android Chrome) — certification is Playwright viewport + touch UA only (`playwright.config.ts` mobile project).
- 320px super-admin drawer FAB overlap not manually re-verified beyond E2E pass.
