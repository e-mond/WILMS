# P13.1 Runtime Warning Audit

**Date:** 2026-06-15  
**Method:** Codebase search + E2E/dev observations (unchanged from P13 where still accurate).

---

## DevRootNotFoundBoundary

| Item | Finding |
| ---- | -------- |
| In app source | **Not found** under `src/` |
| Origin | Next.js App Router internal dev overlay |
| App responsible? | **No** — not referenced in application code |
| Fixed? | N/A — `npm run build` succeeds |

---

## Deferred DOM Node warning

| Item | Finding |
| ---- | -------- |
| In app source | **Not found** |
| Reproducibility | **Not reproduced** in P13.1 validation runs |
| App responsible? | **Unverified** |
| Fixed? | N/A |

---

## runtime.lastError browser warning

| Item | Finding |
| ---- | -------- |
| In app source | **Not found** |
| Typical cause | Browser extension async messaging |
| App responsible? | **No** |
| Fixed? | Not app scope |

---

## Stale `.next` chunk 404 issues

| Item | Finding |
| ---- | -------- |
| Cause | Dev server on wrong port serving outdated chunks while tests hit different port |
| Mitigation | E2E defaults to port **3001**; `CI=1` disables `reuseExistingServer` |
| P13.1 action | Ports 3000/3001 cleared before CI E2E runs |
| App responsible? | **Environment/config**, not application logic |
| Status | Mitigated for CI; developer must avoid conflicting dev servers on 3001 |

---

## Playwright / Node warnings observed

| Warning | Cause | App responsible? |
| ------- | ----- | ---------------- |
| `NO_COLOR` ignored due to `FORCE_COLOR` | Playwright/npm on Windows | **No** |
| jsdom CSS `@page` parse errors in unit tests | Export print CSS in jsdom | **Test env limitation**; print engine tests still pass |

---

## Summary

No new runtime defects attributable to application code were identified in P13.1. E2E login failures were **test harness timing** (bootstrap) and **product app-lock gate** (resolved in `e2e/helpers/auth.ts`), not React/Next runtime warnings.
