# P14.3A  API Coverage Report

**Phase:** P14.3A  
**Date:** 2026-06-09  
**Status:** Complete

---

## Versioning

All business routes mount at **`/api/v1/*`** and **legacy aliases** (no prefix) for P14.2 frontend parity.

**Evidence:** `apps/backend/src/http/app.ts`  `mountBusinessRoutes(app, '/api/v1')` and `mountBusinessRoutes(app, '')`.

---

## Loan Endpoints

| Spec endpoint | Legacy alias | Method | Permission | Status |
|---------------|--------------|--------|------------|--------|
| `/api/v1/loans` | `/loans` | GET | VIEW_FINANCIAL_REPORTS |  |
| `/api/v1/loans/{id}` | `/loans/:id` | GET | VIEW_FINANCIAL_REPORTS |  |
| `/api/v1/loans` | `/loans` | POST | APPROVE_LOANS |  |
| `/api/v1/loans/{id}/approve` | `/loans/:id/approve` | PATCH | APPROVE_LOANS |  |
| `/api/v1/loans/{id}/reject` | `/loans/:id/reject` | PATCH | REJECT_LOANS |  |
| `/api/v1/loans/{id}/disburse` | `/loans/:id/disburse` | POST | APPROVE_LOANS |  |
| `/api/v1/loans/{id}/schedule` | `/loans/:id/schedule` | GET | VIEW_FINANCIAL_REPORTS |  |

**Additional frontend routes (unchanged contracts):**

| Route | Method | Status |
|-------|--------|--------|
| `/loans/portfolio` | GET |  |
| `/loans/:id/progress` | GET |  |
| `/loans/:id/payments` | GET |  |
| `/borrowers/loan-eligible` | GET |  |
| `/borrowers/:id/loans` | GET |  |
| `/borrowers/:id/disbursement-eligibility` | GET |  |

**Evidence:** `apps/backend/src/modules/loans/routes.ts`.

---

## Payment Endpoints

| Spec endpoint | Legacy alias | Method | Permission | Status |
|---------------|--------------|--------|------------|--------|
| `/api/v1/payments` | `/payments` | POST | RECORD_COLLECTIONS |  |
| `/api/v1/payments/{id}` | `/payments/:id` | GET | RECORD_COLLECTIONS |  |

**Additional (P14.2):**

| Route | Method | Status |
|-------|--------|--------|
| `/borrowers/:id/payment-entry` | GET |  (DB engine when DATABASE_URL set) |
| `/payments/same-day` | GET |  (memory/legacy) |
| `/payments/:id` | PATCH |  (legacy edit stub) |

**Evidence:** `apps/backend/src/modules/payments/routes.ts`.

---

## Envelope

All handlers use `sendData()` / `AppError`  unchanged P14.2 envelope.

**Evidence:** `apps/backend/src/http/response.ts`, `http/errors.ts`.

---

## Idempotency Headers

| Endpoint | Header |
|----------|--------|
| `POST /loans/:id/disburse` | `Idempotency-Key` |
| `POST /payments` | `Idempotency-Key` |

Duplicate completed  replay body. In-flight  HTTP 409 CONFLICT.

**Evidence:** `infrastructure/idempotency/run-with-idempotency.ts`.

---

## Database Requirement

Loan and payment engine routes require `DATABASE_URL`. Legacy memory fallback remains for non-loan payment stubs when DB disabled (preserves local dev without Neon).

**Evidence:** `isDatabaseEnabled()` guards in `loans/service.ts`, `payments/routes.ts`.
