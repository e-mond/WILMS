# P14.3A.2  Neon Connectivity Verification

**Phase:** P14.3A.2 Phase 1  
**Date:** 2026-06-09  
**Status:** Partial  connectivity confirmed; seed failed

---

## Prerequisites

| Item | Status |
|------|--------|
| Neon project exists | Assumed (connection string in `.env.example`) |
| Local `.env` file | ﯔ Not present |
| `DATABASE_URL` in runtime |  Set from `.env.example` for verification commands only |
| Developer Neon credentials | Present in `.env.example` (rotate if exposed) |

---

## Commands Executed

Environment loaded from root `.env.example` `DATABASE_URL` line (PowerShell, 2026-06-09):

```powershell
$env:DATABASE_URL = (Get-Content .env.example | Where-Object { $_ -match '^DATABASE_URL=' } | ForEach-Object { $_ -replace '^DATABASE_URL=','' })
npm run db:migrate -w @wilms/api
npm run db:seed -w @wilms/api
npm run verify:financial -w @wilms/api
```

---

## Migration Results

| Check | Result | Evidence |
|-------|--------|----------|
| Neon reachable |  Pass | Drizzle connected via `@neondatabase/serverless` WebSocket driver |
| Migrations apply |  Pass | Exit code 0  `[] migrations applied successfully!` |
| Migration files | 3 | `0000_init.sql`, `0001_tired_sir_ram.sql`, `0002_sad_bucky.sql` |

Schema tables expected after migration include: `loans`, `loan_schedules`, `loan_disbursements`, `ledger_entries`, `idempotency_keys`, `payments` (extended), `roles`, `permissions`, `users`, `borrowers`, `audit`, etc.

---

## Seed Results

| Check | Result | Evidence |
|-------|--------|----------|
| `db:seed` (initial) | ﯔ Failed | SEED-001  non-UUID role IDs |
| `db:seed` (after fix) |  **Pass** | `Database seed completed (RBAC + financial core).` |
| Financial seed data |  Loaded | 4 borrowers, 4 loan scenarios in Neon |
| RBAC seed |  Pass | After role IDs changed to UUID v7 constants |

### SEED-001 (fixed during P14.3A.2)

**Original failure:** `invalid input syntax for type uuid: "role-super-admin"`

**Fix:** `run-seed.ts`  role IDs now use stable UUIDs (`01930000-0001-7000-8000-`).

### Post-seed verification note

Loan `000003` was partially disbursed during an idempotency harness run (pre-fix). Idempotency replay test now skips with `pending seed loan already disbursed`. Re-seed on a fresh database restores pending-loan idempotency coverage.

---

## Repository Layer  PostgreSQL Usage

When `DATABASE_URL` is set, verified code paths use Drizzle repositories:

| Layer | PostgreSQL when DB enabled |
|-------|---------------------------|
| `db/client.ts` | Neon Pool + `drizzle-orm/neon-serverless` |
| `repositories/loan.repository.ts` |  |
| `repositories/payment.repository.ts` |  |
| `repositories/ledger.repository.ts` |  |
| `repositories/idempotency.repository.ts` |  |
| `modules/loans/service.ts` |  DB-backed |
| `modules/payments/service.ts` |  DB-backed |
| `db/persistence.ts` | Falls back to in-memory when `DATABASE_URL` unset |

---

## Financial Verification Harness (with DATABASE_URL)

Final run after SEED-001 fix + idempotency ordering fix (`disburseLoan`):

```
Unit Checks:        12/12 passed
Security / RBAC:     6/6 passed
Database Checks:    36/41 passed
Total:              54/59 passed
```

### DB check failures (5)

| Check | Result | Detail |
|-------|--------|--------|
| `concurrent-repayment-10/25/50` | ﯔ | 2 successes vs expected 1 (optimistic lock rejects most; 2 slipped through) |
| `loan-list-p95-ms` | ﯔ | p95=541ms (target <250ms)  Neon network latency |
| `schedule-retrieval-p95-ms` | ﯔ | p95=1110ms (target <300ms)  Neon network latency |

### Fixes applied during P14.3A.2

| ID | Fix |
|----|-----|
| SEED-001 | UUID role IDs in `run-seed.ts` |
| IDEM-001 | Move `disburseLoan` validation inside `runWithIdempotency` execute block so replay works |

---

## Neon Connected?

| Question | Answer |
|----------|--------|
| Is Neon reachable from this environment? | **Yes**  migrations succeeded |
| Is Neon connected at backend runtime by default? | **No**  no `.env` loaded; existing process on `:4000` likely in-memory |
| Is financial data seeded in Neon? | **Yes**  after SEED-001 fix |

---

## Required Developer Action

1. ~~**Fix seed defect**~~  SEED-001 fixed (UUID role IDs)

2. **Place `DATABASE_URL`** in repo root `.env` or export before commands:

   ```text
   C:\Users\eddie\Downloads\WILMS\.env
   ```

   ```env
   DATABASE_URL=postgresql://USER:PASSWORD@HOST/neondb?sslmode=require
   ```

3. **Re-run after fix:**

   ```bash
   npm run db:migrate -w @wilms/api
   npm run db:seed -w @wilms/api
   npm run verify:financial -w @wilms/api
   ```

4. **Start backend with persistence:**

   ```powershell
   # load .env into process, then:
   npm run dev:api
   ```

   Confirm log line: `WILMS API listening on http://127.0.0.1:4000 [postgresql (Neon)]`

5. **Rotate Neon password** if `.env.example` credential was committed to version control.
