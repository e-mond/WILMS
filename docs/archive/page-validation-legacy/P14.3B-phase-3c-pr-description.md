# PR: [P14.3B-Phase-3C.1] Payment Reversal MVP

**Base:** `release/p14.3a`  
**Head:** `feature/p14.3b-phase-3c-implementation`

---

## Summary

Implements **payment reversal only** (Phase 3C.1 MVP) per approved architecture docs. Compensating `REVERSAL` ledger entries, atomic transaction (reversal row  payment `REVERSED`  schedule revert  balance credit  history  audit), RBAC-protected `POST /payments/:paymentId/reverse`, and idempotency scope `REVERSAL_EXECUTE`.

**Out of scope:** disbursement reversal, pool allocation reversal, adjustment reversal, write-off reversal.

---

## Schema (Phase 1)

| Object | Purpose |
|--------|---------|
| `0005_financial_reversals.sql` | Migration |
| `financial_reversals` | Reversal workflow record |
| `reversal_history` | Append-only before/after trail |
| `REVERSAL` ledger type | Compensating entry |
| `payments.reversed_at`, `reversal_id` | Source flag (no amount mutation) |
| `ledger_entries.reverses_ledger_entry_id`, `reversal_id` | Ledger linkage |

---

## Implementation

| Layer | Files |
|-------|-------|
| Repository | `reversal.repository.ts`, extensions to payment/ledger/schedule repos |
| Service | `payment-reversal.service.ts` |
| API | `POST /payments/:paymentId/reverse`  `ACCESS_ADMIN_PORTAL`, Zod, idempotency |
| Verification | `verify:reversals` (19 checks), `perf:reversals` |

---

## Safety

- Duplicate reversal blocked (`REVERSAL_DUPLICATE` / unique executed source index)
- Approved `PAYMENT_CORRECTION` conflict blocked
- Optimistic locking on payment, schedule, reversal row
- Idempotency replay verified
- Concurrency: single execute per payment
- RBAC: collector cannot reverse (security harness)

---

## Validation

```bash
npm run type-check
npm run lint
npm run build
npm run test
npm run db:migrate -w @wilms/api
npm run verify:reversals -w @wilms/api
npm run verify:financial -w @wilms/api
```

---

## Labels

`backend`, `financial`, `reversals`, `phase-3`, `implementation`

## Milestone

`P14.3B Financial Controls`

---

## STOP

Do not merge additional reversal types until explicitly approved after this PR.
