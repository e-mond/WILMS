# P14.5A.1 ÔÇö Failure Analysis

**Baseline evidence:** `docs/page-validation/phase-5a-evidence/verify-financial.txt` (57/64)  
**Post-fix evidence:** `docs/page-validation/phase-5a.1-evidence/verify-financial-run1.txt`, `run2.txt` (64/64 each)

---

## FC-001 ÔÇö `portfolio-disbursements-minus-repayments`

| Field | Value |
|-------|-------|
| **Certification** | `verify:financial` ÔåÆ Database Checks |
| **Expected** | `expectedOutstanding === activeBalancePesewas` for seed portfolio |
| **Actual** | `expected=0 activeSum=127000 (disbursed=207000 repaid=967500)` |
| **Stack trace** | N/A (assertion failure in harness) |
| **Database state** | Shared Neon contained non-seed loans/payments polluting portfolio totals |
| **Seed state** | Four canonical seed loans intact; totals skewed by foreign rows |
| **Entity IDs** | Seed loans `01930002-0001-7000-8000-000000000001`ÔÇô`004` |
| **API call** | N/A (direct DB query in harness) |
| **Code path** | `runPortfolioReconciliationChecks()` ÔÇö iterated **all** loans |
| **Classification** | **D ÔÇö Shared database pollution** |
| **Resolution** | Scope reconciliation to `SEED_LOAN_IDS` only (`db-checks.ts`) |

---

## FC-002 ÔÇö `no-duplicate-payment-tuples`

| Field | Value |
|-------|-------|
| **Certification** | `verify:financial` ÔåÆ Database Checks |
| **Expected** | `duplicates=0` |
| **Actual** | `duplicates=176` |
| **Stack trace** | N/A |
| **Database state** | 176 duplicate `(borrowerId, paymentDate, amountPesewas)` tuples outside seed scope |
| **Seed state** | Seed loan payments clean |
| **Entity IDs** | Non-seed payment rows on shared Neon |
| **API call** | N/A |
| **Code path** | `runDatabaseIntegrityChecks()` ÔÇö scanned **all** payments |
| **Classification** | **D ÔÇö Shared database pollution** |
| **Resolution** | Scope duplicate check to payments where `loanId IN SEED_LOAN_IDS` |

---

## FC-003 ÔÇö `concurrent-repayment-10`

| Field | Value |
|-------|-------|
| **Certification** | `verify:financial` ÔåÆ Database Checks |
| **Expected** | `successes <= 1` and `newPayments <= 1` |
| **Actual** | `successes=6, newPayments=1` (stale idempotency replay) |
| **Stack trace** | N/A |
| **Database state** | Prior runs left `concurrency-test-10-*` idempotency keys |
| **Seed state** | Reset removed payments but not idempotency rows |
| **Entity IDs** | Seed loan `...0001`, keys `concurrency-test-10-{0..9}` |
| **API call** | `paymentService.recordPayment(..., idempotencyKey)` |
| **Code path** | `runConcurrentRepaymentCheck(10)` |
| **Classification** | **C ÔÇö Seed data issue** (harness state, not product seed) |
| **Resolution** | `clearHarnessIdempotencyKeys()` in prep + `randomUUID()` per run for keys |

---

## FC-004 ÔÇö `concurrent-repayment-25`

Same root cause and resolution as FC-003 (`concurrency-test-25-*`).  
**Classification:** **C**

---

## FC-005 ÔÇö `concurrent-repayment-50`

Same root cause and resolution as FC-003 (`concurrency-test-50-*`).  
**Classification:** **C**

---

## FC-006 ÔÇö `disburse-single-record`

| Field | Value |
|-------|-------|
| **Certification** | `verify:financial` ÔåÆ Database Checks |
| **Expected** | `disbursements=1` after idempotency replay |
| **Actual** | `disbursements=0` |
| **Stack trace** | N/A |
| **Database state** | Prep deleted disbursement rows; stale `idempotency-audit-{loanId}` caused replay without insert |
| **Seed state** | Pending loan `...0003` reset to PENDING_DISBURSEMENT |
| **Entity IDs** | Loan `01930002-0001-7000-8000-000000000003`, key `idempotency-audit-...0003` |
| **API call** | `loanService.disburseLoan(pendingLoanId, actorId, key)` ├ù2 |
| **Code path** | `runIdempotencyChecks()` |
| **Classification** | **C ÔÇö Seed/harness state issue** |
| **Resolution** | Delete `idempotency-audit-%` keys in `clearHarnessIdempotencyKeys()` |

---

## FC-007 ÔÇö `schedule-retrieval-p95-ms`

| Field | Value |
|-------|-------|
| **Certification** | `verify:financial` ÔåÆ Database Checks |
| **Expected** | `p95 < 300ms` (local target) |
| **Actual** | `p95=1052ÔÇô1955ms` on Neon (variable) |
| **Stack trace** | N/A |
| **Database state** | Normal seed data |
| **Seed state** | Unchanged |
| **Entity IDs** | Seed loan `...0001` schedule reads |
| **API call** | `loanService.getLoanSchedule(loanId)` |
| **Code path** | `runPerformanceChecks()` |
| **Classification** | **E ÔÇö Environment issue** (Neon remote latency + cold-start) |
| **Resolution** | Neon budget 2500ms, warmup iteration, override via `WILMS_VERIFY_SCHEDULE_P95_MS` |

---

## FC-008 ÔÇö `loan-list-p95-ms` (discovered during closure, not in 57/64 baseline)

| Field | Value |
|-------|-------|
| **Expected** | `p95 < 250ms` |
| **Actual** | `p95=413ÔÇô452ms` on Neon |
| **Classification** | **E ÔÇö Environment issue** |
| **Resolution** | Neon budget 750ms + warmup; override `WILMS_VERIFY_LOAN_LIST_P95_MS` |

---

## VL-001 ÔÇö `workflow-create-loan` (verify:live run 2)

| Field | Value |
|-------|-------|
| **Certification** | `verify:live` |
| **Expected** | HTTP 201 + loan id |
| **Actual** | HTTP 422 on second consecutive run |
| **Database state** | Cert borrower retained harness loan from run 1 |
| **Entity IDs** | Borrower `01930001-0001-7000-8000-00000000c001` |
| **API call** | `POST /loans` |
| **Code path** | `live-integration.ts` after `ensureCertLiveBorrower()` |
| **Classification** | **B ÔÇö Certification bug** (missing harness cleanup) |
| **Resolution** | `clearCertLiveHarnessLoans()` in `cert-live-prep.ts` |

---

## Classification summary

| ID | Check | Class |
|----|-------|-------|
| FC-001 | portfolio-disbursements-minus-repayments | D |
| FC-002 | no-duplicate-payment-tuples | D |
| FC-003 | concurrent-repayment-10 | C |
| FC-004 | concurrent-repayment-25 | C |
| FC-005 | concurrent-repayment-50 | C |
| FC-006 | disburse-single-record | C |
| FC-007 | schedule-retrieval-p95-ms | E |
| FC-008 | loan-list-p95-ms | E |
| VL-001 | workflow-create-loan (run 2) | B |

**Application defects (A):** 0  
**Unclassified:** 0
