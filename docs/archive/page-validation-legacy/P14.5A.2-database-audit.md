# P14.5A.2 ÔÇö Database Audit

**ORM:** Drizzle 0.38  
**Migrations:** `apps/backend/src/db/migrations/` ÔÇö journal entries **7** (`0000`ÔÇô`0006`)

---

## Migration inventory

| File | Purpose |
|------|---------|
| `0000_init.sql` | Core schema: users, borrowers, loans, payments, schedules, ledger, groups, audit |
| `0001_tired_sir_ram.sql` | Incremental |
| `0002_sad_bucky.sql` | Incremental |
| `0003_loan_pools.sql` | Loan pools |
| `0004_financial_adjustments.sql` | Adjustments |
| `0005_financial_reversals.sql` | Reversals |
| `0006_financial_reconciliations.sql` | Reconciliations |

Health check reads `__drizzle_migrations` count vs journal (`health.service.ts` L82ÔÇô88).

---

## Schema tables (`db/schema/index.ts`)

| Table module | Used by repository/service | Status |
|--------------|---------------------------|--------|
| users, rbac | userRepository, auth | Ô£à Active |
| borrowers | borrowerRepository, borrowerService | Ô£à Active |
| loans, loan_schedules, loan_disbursements | loanRepository, loanService | Ô£à Active |
| payments, ledger_entries | paymentService, ledgerRepository | Ô£à Active |
| idempotency_keys | idempotency.repository | Ô£à Active |
| loan_pools | loan-pool.repository | Ô£à Active |
| financial_adjustments | adjustment service | Ô£à Active |
| financial_reversals | reversal service | Ô£à Active |
| financial_reconciliations | reconciliation service | Ô£à Active |
| groups, group_members | group.repository, formation, cert prep | Ô£à Partial ÔÇö no CRUD API |
| uploads | upload.repository | Ô£à Active |
| audit | audit-log (in-memory + optional persist) | Ô£à Active |
| expenses | **schema only** | ÔÜá´©Å **Orphan** ÔÇö no routes |
| notifications | **schema only** | ÔÜá´©Å **Orphan** ÔÇö no routes |

---

## Seed & certification reset

| Script | Purpose | Deterministic |
|--------|---------|---------------|
| `db/seed/seed-financial.ts` | Four canonical seed loans | Ô£à |
| `cert-financial-prep.ts` | Reset seed loans + idempotency cleanup | Ô£à P14.5A.1 |
| `cert-live-prep.ts` | Cert borrower + harness loan cleanup | Ô£à P14.5A.1 |
| `cert-reversal-seed-reset.ts` | Reversal harness pollution | Ô£à |

Shared Neon pollution mitigated by seed-scoped cert checks (P14.5A.1).

---

## Enums

Centralized in `db/schema/enums.ts` and mirrored in `@wilms/shared-contracts`. Financial enums (loan status, schedule status, reversal types) align with domain logic in `domain/loan/lifecycle.ts`.

---

## Persistence dual-mode

| Mode | Trigger | Production suitability |
|------|---------|---------------------|
| PostgreSQL | `DATABASE_URL` set | Ô£à Required production |
| In-memory | `DATABASE_URL` unset | ÔØî Dev/demo only ÔÇö `db/persistence.ts`, payment fallbacks |

`validate-env.ts` errors if `DATABASE_URL` missing in `NODE_ENV=production`.

---

## Foreign key cleanup (cert harness)

Cert prep scripts delete in order: reversals ÔåÆ adjustments ÔåÆ pool allocations ÔåÆ ledger ÔåÆ payments ÔåÆ schedules ÔåÆ disbursements ÔåÆ loans (`cert-financial-prep.ts`). Verified idempotent on double-run (P14.5A.1).

---

## Database risks

| ID | Issue | Severity |
|----|-------|----------|
| DB-001 | `expenses` + `notifications` tables migrated but no API ÔÇö schema drift | Medium |
| DB-002 | Shared Neon accumulates non-seed rows ÔÇö cert scoped, not prod isolation | Medium |
| DB-003 | `cert-upload-smoke` uses invalid UUID for `entityId` column | High (cert) |
| DB-004 | No automated migration run in CI visible from repo | Low |

No database changes in this audit.
