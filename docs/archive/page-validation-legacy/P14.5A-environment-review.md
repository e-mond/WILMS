# P14.5A  Environment Review

**Files aligned:** `.env.example`, `apps/backend/.env.example`, `apps/backend/src/config/env.ts`, `validate-env.ts`

## New variables (P14.5A)

| Variable | Purpose |
|----------|---------|
| `WILMS_GIT_COMMIT` | Health report git commit |
| `WILMS_TRUST_PROXY` | Behind reverse proxy |
| `WILMS_TRUST_PROXY_HOPS` | Proxy hop count |

## Production validation (`validate-env.ts`)

Fails startup in production when: missing `DATABASE_URL`, `WILMS_CORS_ORIGIN`, `WILMS_SESSION_SECRET`, or invalid upload config.

## Status

**PASS** (code + docs). Staging values not configured in repo.
