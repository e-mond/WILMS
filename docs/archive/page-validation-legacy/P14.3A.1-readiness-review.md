# P14.3A.1  Readiness Review

**Phase:** P14.3A.1 Phase 0  
**Date:** 2026-06-09  
**Status:** Complete  code-verified; see `P14.3A.1-repository-discovery.md` for document inventory

---

## Prerequisite Documentation

All expected P14.1AP14.3A documents **exist on disk**. None missing. See repository discovery for full file list.

---

## Summary

| Area | Classification | Notes |
|------|----------------|-------|
| Loan schema (`loans.ts`) | **Safe** | NUMERIC(15,2), soft delete, version, lifecycle + external status |
| Schedule persistence | **Safe** | Composite PK, optimistic version, status enum |
| Disbursement persistence | **Safe** | Append-only insert repo; immutable by design |
| Ledger persistence | **Safe** | Append-only; 4 entry types; GHS default |
| Audit persistence | **Needs Attention  Fixed** | Loan actions previously mapped to `SETTINGS_EXPORTED`; fixed in P14.3A.1 |
| Idempotency | **Needs Attention  Fixed** | Unique-index race could throw 500; now returns 409 |
| Concurrency | **Safe** | Optimistic `version` on loans + schedule weeks |
| Repository transactions | **Safe** | `runInTransaction()` used for create/disburse/repay |
| API contract compliance | **Safe** | Legacy + `/api/v1` mounts; camelCase pesewas DTOs preserved |
| Seed data coverage | **Safe** | Active, completed, pending, defaulted scenarios |

---

## Schema Evidence

| Table | File | Key constraints |
|-------|------|-----------------|
| `loans` | `apps/backend/src/db/schema/loans.ts` | FK borrowers/users; soft delete columns |
| `loan_schedules` | `loan-schedules.ts` | PK (loan_id, week_number) |
| `loan_disbursements` | `loan-disbursements.ts` | FK loan, disbursed_by_user |
| `ledger_entries` | `ledger-entries.ts` | FK loan, borrower, payment |
| `idempotency_keys` | `idempotency-keys.ts` | Unique (scope, actor, key) |
| `payments` | `payments.ts` | FK loan; schedule_week_number |

Migration chain: `0000_init.sql`, `0001_tired_sir_ram.sql`, `0002_sad_bucky.sql` (audit enum extension).

---

## Idempotency Implementation

| Component | Path | Status |
|-----------|------|--------|
| Begin / complete | `repositories/idempotency.repository.ts` | Safe after unique-violation handling |
| Wrapper | `infrastructure/idempotency/run-with-idempotency.ts` | Safe; deletes in-progress key on failure |
| Scopes | LOAN_DISBURSE, PAYMENT_POST | Used on disburse + payment POST |

---

## Concurrency Implementation

| Entity | Mechanism | Failure |
|--------|-----------|---------|
| `loans.version` | Conditional UPDATE | `CONFLICT:Loan was modified` |
| `loan_schedules.version` | Conditional UPDATE | `CONFLICT:Schedule week was modified` |

**Defect fixed:** Payment routes checked `error.message === 'CONFLICT'` but repositories throw `CONFLICT:` prefix. Unified via `mapFinancialRouteError()`.

---

## API Contract Compliance

Verified against `P14.3A-dto-alignment-report.md` and `P14.2-frontend-contract-verification.md`:

* No field renames
* 5-value `LOAN_STATUS` via `external_status`
* Payment entry context shape preserved when DB enabled

---

## Seed Coverage

| ID suffix | Scenario | Evidence |
|-----------|----------|----------|
| 0001 | Active, 3/10 paid | `seed-financial.ts` |
| 0002 | Completed, 8/8 paid | same |
| 0003 | Pending disbursement | same |
| 0004 | Defaulted, 4 paid + 2 missed | same |

---

## High Risk Items (Resolved in P14.3A.1)

| Item | Resolution |
|------|------------|
| Concurrent payment 409 mapped to 500 | `http/map-financial-error.ts` |
| Idempotency insert race | Unique violation  `IDEMPOTENCY_IN_PROGRESS` |
| Loan audit misclassification | Enum + ACTION_MAP extension |

---

## Verification Harness

```bash
npm run verify:financial -w @wilms/api
```

Unit + security checks run without DATABASE_URL. DB checks require Neon + seed.

**Evidence (local run):** 18/18 passed (12 unit, 6 security) with DB skipped.
