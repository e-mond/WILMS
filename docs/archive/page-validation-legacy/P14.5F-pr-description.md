## Summary

- Migrate production frontend to **https://wilms.vercel.app** (alias + env + CORS)
- Security audit with `X-Powered-By` suppression
- Health endpoint migration count fix (`drizzle.__drizzle_migrations`)
- Login page version badge from root `package.json`
- P14.5F evidence and deployment documentation

## Files changed

| Area | Files |
|------|-------|
| Domain / deploy docs | `README.md`, `docs/page-validation/P14.5F-*.md` |
| Health | `apps/backend/src/modules/health/health.service.ts` |
| Version badge | `apps/frontend/next.config.mjs`, `AppVersionBadge.tsx`, `login/page.tsx` |
| Security | `poweredByHeader: false` in `next.config.mjs` |

## Security improvements

- CORS locked to `https://wilms.vercel.app`
- Vercel SSO deployment protection disabled for public production access
- Frontend `X-Powered-By` removed (pending deploy)
- CSRF: assessed, **not implemented**  defer recommended pending decision

## Domain change

- **Before:** `https://frontend-ashen-gamma-11.vercel.app`
- **After:** `https://wilms.vercel.app` (smoke **12/12 PASS**)

## Performance findings

- API health warm ~813ms; BFF health warm ~1453ms (see `P14.5F-performance-report.md`)

## Remaining risks

- Demo seed users still in production Neon (**STOP GATE 2**)
- CSRF not implemented (**STOP GATE 3**)
- Health migration fix not deployed to Railway yet
- Legacy Vercel alias still active

## Test plan

- [ ] `npm run type-check && npm run lint && npm run build && npm run test`
- [ ] `WILMS_APP_URL=https://wilms.vercel.app WILMS_API_URL=https://wilms-production.up.railway.app npm run smoke:production`
- [ ] Verify `/health` shows `migrations.applied: 7` after Railway redeploy
- [ ] Verify login page shows `v0.1.0` after Vercel redeploy
- [ ] Confirm demo login fails after credential replacement (post STOP GATE 2)

**Do not merge without human review.**
