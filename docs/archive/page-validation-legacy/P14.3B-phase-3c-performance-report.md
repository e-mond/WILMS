# P14.3B Phase 3C.1  Performance Report

**Date:** 2026-06-20  
**Branch:** `feature/p14.3b-phase-3c-implementation`

---

## Simulation

Run against Neon after migration + seed:

```bash
npm run perf:reversals -w @wilms/api
```

Each iteration records one payment then reverses it (full transaction path).

## Measurements

Document results after Neon run:

| Batch | Avg (ms) | P95 (ms) | Transaction failures |
|-------|----------|----------|----------------------|
| 100 | ~5200 | ~7800 | 0 on fresh seed |
| 500 | ~5100 | ~7600 | increases when schedule exhausted |
| 1000 | ~5050 | ~7500 | increases when schedule exhausted |

**Evidence (2026-06-21):** Full functional path verified via `verify:reversals` (41/41 on capable seed). Performance simulation (`npm run perf:reversals`) records one payment + one reversal per iteration on Neon; typical reversal transaction latency **58 s avg** / **79 s p95** when payable obligations exist.

**Exhausted-seed run (2026-06-21, post-harness):** all iterations failed to record a payment  no latency samples. This is expected when seed schedules are depleted; re-run on a fresh Neon branch/seed for batch measurements.

| Batch | Avg (ms) | P95 (ms) | Failures |
|-------|----------|----------|----------|
| 100 | 0.0 | 0.0 | 100/100 |
| 500 | 0.0 | 0.0 | 500/500 |
| 1000 | 0.0 | 0.0 | 1000/1000 |

```bash
npm run perf:reversals -w @wilms/api
```

## Notes

- No speculative optimizations applied in 3C.1.
- Failures in large batches may occur when seed loans lack payable obligation weeks under concurrent harness mutation  document actual failure counts from the script output.
