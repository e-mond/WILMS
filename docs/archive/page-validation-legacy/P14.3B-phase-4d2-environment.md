# P14.3B.4D.2  Live Environment Verification

**Date:** 2026-06-20  
**Verdict:** **PASS** (local demo stack)

---

## Backend

| Check | Result | Evidence |
|-------|--------|----------|
| Running | PASS | `GET http://127.0.0.1:4000/health`  200 |
| Database | PASS | `DATABASE_URL` configured (Neon) |
| Authentication | PASS | Demo users login via `POST /auth/login` |
| Upload provider | PASS | `cert:upload:env`  `active_provider: local` |
| Cloudinary | **NOT CONFIGURED** | `cloudinary_configured: false` |

**Backend URL:** `http://127.0.0.1:4000`  
**CORS origin:** `http://127.0.0.1:3000`

---

## Frontend

| Check | Result | Evidence |
|-------|--------|----------|
| Running | PASS | `GET http://127.0.0.1:3000`  200 |
| Live API mode | PASS | `apps/frontend/.env.local`  `NEXT_PUBLIC_USE_MOCK=false` |
| API base URL | PASS | `http://127.0.0.1:3000/api/wilms` |
| Proxy upstream | PASS | `WILMS_API_UPSTREAM=http://127.0.0.1:4000` |

**Frontend URL:** `http://127.0.0.1:3000`  
**API mode:** `api` (when `.env.local` loaded; restart required after env change)

---

## Configuration Summary

| Variable | Value |
|----------|-------|
| `UPLOAD_PROVIDER` | `local` (default) |
| `NEXT_PUBLIC_USE_MOCK` | `false` (demo `.env.local`) |
| `NEXT_PUBLIC_API_BASE_URL` | `http://127.0.0.1:3000/api/wilms` |

---

## Discrepancies

| Item | Note |
|------|------|
| Cloudinary | Production profile expects `cloudinary`; demo uses local disk + PostgreSQL metadata |
| Frontend without `.env.local` | Defaults to mock  **not** demo path |
| Backend logout | No `/auth/logout`; frontend clears session cookie |

---

## Demo Prerequisites Checklist

```bash
# Terminal 1  backend
npm run start -w @wilms/api

# Terminal 2  frontend (copy .env.local.example  .env.local, set USE_MOCK=false)
npm run dev -w @wilms/frontend
```
