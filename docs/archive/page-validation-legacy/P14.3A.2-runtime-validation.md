# P14.3A.2 ÔÇö Runtime Validation

**Phase:** P14.3A.2 Phase 5  
**Date:** 2026-06-09  
**Status:** Complete

---

## Validation Commands

| Command | Result | Evidence |
|---------|--------|----------|
| `npm run type-check` | Ô£à Pass | Frontend + `@wilms/api` tsc clean |
| `npm run lint` | Ô£à Pass | `next lint` ÔÇö no warnings/errors |
| `npm run build` | Ô£à Pass | 42 routes, exit 0 (~67s) |
| `npm run test` | Ô£à Pass | 400 tests (200+200 shards), exit 0 (~578s) |
| `CI=1 npm run test:e2e` | ÔØî **Not completed** | Port conflict ÔÇö see below |
| `npm run db:migrate -w @wilms/api` | Ô£à Pass | With `DATABASE_URL` from `.env.example` |
| `npm run db:seed -w @wilms/api` | Ô£à Pass (after SEED-001 fix) | RBAC + financial core |
| `npm run verify:financial -w @wilms/api` | ÔÜá´©Å Partial | 54/59 passed (concurrency + Neon latency) |

---

## E2E Failure (this session)

```
Error: http://127.0.0.1:3001/login is already used,
make sure that nothing is running on the port/url
or set reuseExistingServer:true in config.webServer.
```

**Cause:** Frontend dev server started on `:3001` before E2E; Playwright webServer could not bind.

**Prior evidence (P14.3A.1):** 185 passed, 1 skipped ÔÇö mock-backed suite.

---

## Dev Server ÔÇö Backend

| Check | Result |
|-------|--------|
| `npm run dev:api` with `DATABASE_URL` | ÔØî `EADDRINUSE` ÔÇö port 4000 already in use |
| Existing process on `:4000` | Ô£à Responding |
| `GET /health` | Ô£à 200 `{"data":{"status":"ok","service":"wilms-api"}}` |
| `GET /loans` (no auth) | Ô£à 401 |
| Persistence mode of existing process | **Unknown** ÔÇö likely in-memory (no `.env` auto-load) |

Attempted startup log (new process):

```
Error: listen EADDRINUSE: address already in use 127.0.0.1:4000
```

---

## Dev Server ÔÇö Frontend

| Check | Result |
|-------|--------|
| `npm run dev -w @wilms/frontend` | Ô£à Started |
| Port | `:3001` (3000 in use) |
| Ready | Ô£à `Ô£ô Ready` after compile |
| URL | `http://localhost:3001` |
| Demo mode | Active (no `NEXT_PUBLIC_API_BASE_URL` in process) |

Startup output:

```text
ÔÜá Port 3000 is in use, trying 3001 instead.
Ôû▓ Next.js 14.2.18
- Local: http://localhost:3001
Ô£ô Starting...
Ô£ô Ready
```

---

## Missing Environment Variables (default dev)

| Variable | Observed behavior |
|----------|-------------------|
| `DATABASE_URL` | Not in process ÔåÆ backend in-memory |
| `NEXT_PUBLIC_API_BASE_URL` | Empty ÔåÆ mock services |
| `WILMS_API_UPSTREAM` | Defaults OK for BFF |
| `.env` file | Absent |

---

## Dependency Failures

None observed for type-check, lint, build, or unit tests.

---

## Route Failures

No route failures during build. Runtime backend auth correctly rejects unauthenticated loan access.

---

## Financial Verification Detail

With `DATABASE_URL` configured:

| Section | Passed |
|---------|--------|
| Unit | 12/12 |
| Security / RBAC | 6/6 |
| Database | 0 ÔÇö process exit on `NOT_FOUND` in performance checks |

---

## Runtime Validation Summary

| Component | Starts | Errors |
|-----------|--------|--------|
| Frontend dev | Ô£à Yes | Port fallback to 3001 |
| Backend dev | ÔÜá´©Å Blocked | Port 4000 occupied; existing instance healthy |
| Neon migrate | Ô£à Yes | ÔÇö |
| Neon seed | ÔØî No | UUID seed defect |
| Full test suite | Ô£à Yes | ÔÇö |
| E2E | ÔØî Blocked this session | Port conflict |
