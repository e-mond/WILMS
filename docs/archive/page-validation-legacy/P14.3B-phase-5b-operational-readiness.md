# P14.3B Phase 5B ÔÇö Operational Readiness & Performance

**Date:** 2026-06-20  
**Status:** **PARTIAL**

Covers Phase 4 (operational) and Phase 5 (performance) per 5B plan.

---

## Phase 4 ÔÇö Operational readiness

| C-ID | Procedure | Source | Result | Evidence |
|------|-----------|--------|--------|----------|
| C-060 | Migration runbook | README + 5A deployment guide | **PASS** | Journal 0000ÔÇô0006 |
| C-061 | Seed after reset | `cert:*:seed-reset` + `db:seed` | **PASS** | Login 200 with demo creds |
| C-062 | Cold start | `GET /health` | **PASS** | `{"data":{"status":"ok","service":"wilms-api"}}` |
| C-063 | Health on DB disconnect | Stop DB / bad URL | **FAIL** | `/health` still 200 ÔÇö no DB probe |
| C-064 | `WILMS_SESSION_SECRET` required in production | `env.ts` | **PASS** | Throws if unset when `NODE_ENV=production` |
| C-065 | 500 sanitization | `error-handler.ts` | **PASS** | Generic client message |
| C-066 | Backup / restore | 5A deployment guide | **PARTIAL** | Neon PITR documented; restore drill not executed |
| C-067 | Unauthenticated API | `GET /loans` | **PASS** | 401 |
| C-068 | Login audit persistence | Post re-seed | **PASS** | bcrypt + signed sessions active |

### Lifecycle procedures verified

| Step | Command / action | Result |
|------|------------------|--------|
| Seed reset (reconciliation) | `npm run cert:reconciliation:seed-reset -w @wilms/api` | Executed pre-cert |
| Seed reset (reversal) | `npm run cert:reversal:seed-reset -w @wilms/api` | Executed pre-cert |
| Full seed | `npm run db:seed -w @wilms/api` | **PASS** |
| Backend restart | Required after seed (bcrypt) | **PASS** |

---

## Phase 5 ÔÇö Performance and reliability

### Latency measurements

| C-ID | Endpoint / flow | Method | p50 | p95 / max | Target | Result |
|------|-----------------|--------|-----|-----------|--------|--------|
| C-070 | `GET /health` | 10├ù PowerShell `Invoke-WebRequest` | ~51ms | max 476ms (cold) | <250ms p95 | **PASS** |
| C-071 | Loan list | `verify:financial` harness | ÔÇö | 209.7ms p95 | <250ms | **PASS** |
| C-072 | Schedule retrieval | `verify:financial` harness | ÔÇö | 622.0ms p95 | <300ms | **FAIL** |

### Concurrency (existing cert scripts)

| C-ID | Domain | Loads | Result |
|------|--------|-------|--------|
| C-073 | Reversal | 10 / 25 / 50 | **PASS** |
| C-074 | Reconciliation | 10 / 25 / 50 | **PASS** |

### Not executed

| C-ID | Item | Status | Rationale |
|------|------|--------|-----------|
| C-075 | 30-minute memory soak | **NOT RUN** | Time-boxed cert session |
| C-076 | Upload perf (Cloudinary) | **NOT RUN** | `UPLOAD_PROVIDER=local` only |
| C-077 | `cert:reversal:perf` / `cert:reconciliation:perf` | **NOT RUN** | Long-running; deferred |

### Known limitations

- Neon remote latency inflates schedule retrieval p95.
- `verify:financial` concurrent-repayment harness fails on polluted shared DB (not isolated cert DB).
- Local upload path does not represent Cloudinary production latency.

---

## Failures & blockers

| Finding | Impact |
|---------|--------|
| Health endpoint blind to DB | Load balancers may route to broken instances |
| Schedule p95 exceeds threshold | UX on schedule-heavy pages in remote DB region |
| No production soak test | Memory leak risk uncharacterized |

---

## Summary

Core **operational procedures** (migrate, seed, boot, auth) are **documented and verified locally**. **Health depth** and **production soak/Cloudinary perf** remain gaps. Performance is **acceptable for health and loan list**; **schedule retrieval** needs monitoring or DB region tuning before strict SLA sign-off.
