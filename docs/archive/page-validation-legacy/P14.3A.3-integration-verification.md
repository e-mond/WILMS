# P14.3A.3  Integration Verification

**Phase:** P14.3A.3 Phase 4  
**Date:** 2026-06-09  
**Status:** Complete (runtime evidence)

---

## Test Environment

- Backend: `npm run start -w @wilms/api` with root `.env` auto-loaded
- Startup log: `[postgresql (Neon)]`, `Upload provider: local`
- Health: `GET http://127.0.0.1:4000/health`  `200`

---

## Integration Matrix

| Domain | Frontend  API | BFF  Backend | Backend  Neon | Status | Evidence |
|--------|----------------|---------------|----------------|--------|----------|
| **Auth** | `/api/auth/login` (Next) | N/A for login | users table when DB on | **Partially Working** | Login route exists; demo tokens |
| **Borrowers** | `borrowerService`  `/borrowers` | Proxy when BFF URL set | `borrowerRepository` | **Partially Working** | Routes + repo verified; dev uses mock |
| **Loans** | `loanService`  `/loans` | Proxy when BFF URL set | `loanRepository` | **Partially Working** | `GET /loans`  401 without auth |
| **Payments** | `paymentService`  `/payments` | Proxy when BFF URL set | `paymentRepository` + ledger | **Partially Working** | Service + harness 54/59 |
| **Uploads** | `uploadService`  `/uploads` | Proxy when BFF URL set | Provider (local/Cloudinary) | **Partially Working** | Routes + validation + signature endpoint |

---

## Runtime Evidence

### Backend + Neon (2026-06-09)

```text
WILMS API listening on http://127.0.0.1:4000 [postgresql (Neon)]
```

Confirms `DATABASE_URL` loaded from `.env` without manual export.

### Health endpoint

```json
{"data":{"status":"ok","service":"wilms-api"}}
```

### Auth enforcement

`GET /loans` without session  `401` (verified prior session + this phase).

### Env loading chain

```text
Frontend (.env.local)  NEXT_PUBLIC_API_BASE_URL
   apiClient  BFF  WILMS_API_UPSTREAM
   Backend (load-env.ts)  DATABASE_URL  Neon
```

**Full stack live test:** Requires `NEXT_PUBLIC_API_BASE_URL` + `NODE_ENV=production` build or env policy change. Default `npm run dev` remains **Mock Only** by design.

---

## Classification Summary

| Status | Domains |
|--------|---------|
| **Working** | Health, env auto-load, Neon persistence at backend startup |
| **Partially Working** | Auth, borrowers, loans, payments, uploads |
| **Mock Only** | All domains in default `npm run dev` |
| **Broken** | None identified |

---

## Production Integration Path

1. Root `.env` + `apps/backend/.env.local` with `DATABASE_URL`
2. `apps/frontend/.env.production` with `NEXT_PUBLIC_API_BASE_URL`  BFF
3. `WILMS_API_UPSTREAM`  backend URL
4. `npm run build` + `npm run start` (frontend) + `npm run start -w @wilms/api`
