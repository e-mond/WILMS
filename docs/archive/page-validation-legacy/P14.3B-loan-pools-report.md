# P14.3B  Loan Pools Report

**Phase:** P14.3B Phase 1  
**Date:** 2026-06-09  
**Status:** Complete

---

## Objective

Implement persistent loan pools with allocation tracking, balance integrity, and read-only API endpoints matching the frontend `ILoanPoolService` contract.

---

## Scope

| In scope | Out of scope (later phases) |
|----------|----------------------------|
| Schema: `loan_pools`, `pool_memberships`, `pool_allocations` | Disburse/payment auto-allocation hooks |
| Migration `0003_loan_pools` | Pool write/mutate API |
| `GET /loan-pools`, `GET /loan-pools/:id` | Simulation at 1000+ pools |
| Seed + `verify:pools` | Performance optimization (Phase 6) |
| `loans.loan_pool_id` nullable FK | Group membership seed (no groups in DB seed yet) |

---

## Files Changed

| Path | Change |
|------|--------|
| `apps/backend/src/db/schema/loan-pools.ts` | New tables |
| `apps/backend/src/db/schema/enums.ts` | Pool status, allocation type, audit values |
| `apps/backend/src/db/schema/loans.ts` | `loan_pool_id` column |
| `apps/backend/src/db/migrations/0003_loan_pools.sql` | Migration + indexes |
| `apps/backend/src/domain/loan-pool/balance.ts` | Aggregate + integrity rules |
| `apps/backend/src/domain/loan-pool/mappers.ts` | Frontend DTO alignment |
| `apps/backend/src/repositories/loan-pool.repository.ts` | Persistence |
| `apps/backend/src/modules/loan-pools/service.ts` | Service layer |
| `apps/backend/src/modules/loan-pools/routes.ts` | HTTP routes |
| `apps/backend/src/db/seed/seed-loan-pools.ts` | Seed data |
| `apps/backend/src/verification/pool-checks.ts` | Verification |
| `apps/backend/src/verification/run-pools.ts` | `verify:pools` runner |
| `apps/backend/src/http/app.ts` | Router mount |
| `docs/page-validation/P14.3B-pool-architecture-review.md` | Pre-implementation gate |

---

## Decisions

1. **Append-only allocations**  Pool movements never update/delete rows; compensating entries in Phase 3.
2. **Denormalized aggregates**  `loan_pools` columns updated at seed/write time; allocations are source of truth for verification.
3. **UUID pool IDs**  Consistent with P14.3A; mock `PL-001` ids not used in API.
4. **`totalPoolFundsPesewas`**  API uses sum of pool `capitalPesewas` (not mock `DEMO_OPERATING_POOL_PESEWAS`).
5. **Read permission**  `VIEW_FINANCIAL_REPORTS` (same as loan list reads).
6. **Disburse integration deferred**  `loan_pool_id` set in seed only; no change to `disburseLoan` transaction.

---

## Risks

| Risk | Status |
|------|--------|
| Pool totals drift from live disbursements | Mitigated by Phase 4+ hooks; seed-only for now |
| Mock/API KPI mismatch | Documented; API mode shows real sums |
| Empty `pool_memberships` | Groups not seeded; `group_count` stored on pool row |

---

## Validation Results

| Gate | Result |
|------|--------|
| `npm run type-check` |  Pass |
| `npm run lint` |  Pass |
| `npm run build` |  Pass (42 routes) |
| `npm run test` |  Pass (400/400) |
| `npm run db:migrate` |  Migration `0003` applied |
| `npm run db:seed` |  3 pools seeded |
| `npm run verify:pools` |  **5/5** |
| `CI=1 npm run test:e2e` | Not required (backend-only; no UI changes) |

### `verify:pools` evidence

```text
## Pool Balance Integrity  1/1
   pool-balance-integrity: all pools reconcile

## Pool API Checks  4/4
   pool-list-non-empty: pools=3
   pool-list-summary-math: summaryDisbursed=122000
   pool-detail-retrieval: activity=5
   pool-allocation-aggregation: disbursed=74000 collected=39000

Total: 5/5 passed
```

---

## API Endpoints

| Method | Path | Permission | Response |
|--------|------|------------|----------|
| GET | `/loan-pools` | `view-financial-reports` | `LoanPoolListResponse` |
| GET | `/loan-pools/:id` | `view-financial-reports` | `LoanPoolDetail` |

Envelope: `{ "data": { ... } }` per ADR API contract.

---

## Recommendations

1. **Phase 2**  Wire disburse/payment services to append pool allocations atomically.
2. **Phase 6**  Re-run `perf:baseline` after pool list joins at scale.
3. **Phase 7**  Simulation: 100 / 1,000 pools, 10,000 allocations.

---

## Architecture Review

Pre-implementation review: `P14.3B-pool-architecture-review.md`  **approved to proceed**.
