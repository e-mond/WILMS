# P14.5A.2 ÔÇö Security Audit

**Scope:** Authentication, sessions, transport headers, uploads, secrets, env validation  
**Method:** Static code review + cert script re-run where noted

---

## Authentication

| Control | Implementation | Status |
|---------|----------------|--------|
| Password hashing | bcrypt via `lib/password.ts` | Ô£à |
| Login validation | Zod `loginApiSchema` | Ô£à |
| Failed login audit | `appendAuditEntry` in auth routes | Ô£à |
| Demo user fallback | `DEMO_USERS` when no DATABASE_URL | ÔÜá´©Å Dev only |
| Session token | HMAC-signed JWT-like token `encodeSessionToken` | Ô£à |
| Session duration | `WILMS_SESSION_DURATION_MS` / env | Ô£à |

---

## Sessions & cookies

| Control | Location | Status |
|---------|----------|--------|
| Frontend session cookie | `lib/auth/cookies.ts`, BFF login route | Ô£à httpOnly pattern |
| Middleware expiry | `parseSessionCookieState` ÔåÆ redirect `/session-expired` | Ô£à |
| Backend session parse | `optionalAuth` / `requireAuth` middleware | Ô£à |
| Logout | BFF clears cookie | Ô£à |

---

## Transport & headers

| Control | Location | Status |
|---------|----------|--------|
| Helmet | `http/app.ts` L42ÔÇô47 | Ô£à CSP enabled in production |
| CORS | Single origin `env.corsOrigin`, credentials true | Ô£à ÔÇö must set per deploy |
| Trust proxy | `WILMS_TRUST_PROXY` + hops | ÔÜá´©Å Off by default |
| JSON body limit | 15mb (upload base64) | Ô£à intentional |
| Rate limiting | Login only ÔÇö 20/15min (`login-rate-limit.ts`) | ÔÜá´©Å No global API rate limit |

---

## Upload security

| Control | Status |
|---------|--------|
| MIME allowlist | Ô£à `UPLOAD_ALLOWED_MIME_TYPES` |
| Size limit | Ô£à `UPLOAD_MAX_SIZE_BYTES` |
| Provider validation | Ô£à `validateUploadEnvironment()` |
| Owner-based access | Ô£à `assertUploadAccess` on GET/delete |
| Cloudinary signed uploads | Ô£à `/uploads/signature` |
| Multipart upload | ÔØî Not implemented ÔÇö dataUrl only |

**cert:upload:env:** PASS (cloudinary configured, secrets redacted in output)  
**cert:upload:smoke:** FAIL ÔÇö `invalid input syntax for type uuid: "cert-smoke"` at `upload.repository.ts` ÔÇö harness passes non-UUID `entityId`

---

## Secrets handling

| Item | Status |
|------|--------|
| `.env` in `.gitignore` | Ô£à |
| Example files stripped of real creds | Ô£à commit `169717b` |
| Env validation no secret logging | Ô£à `validate-env.ts` |
| Local `.env` contains live Cloudinary + Neon credentials | ÔÜá´©Å Developer hygiene ÔÇö not in git |

---

## Environment validation

`validateEnvironment()` (`config/validate-env.ts`):

- Production requires: `DATABASE_URL`, `WILMS_CORS_ORIGIN`, non-default `WILMS_SESSION_SECRET`
- Upload provider validation merged from upload env module
- **Warnings** do not block dev startup; **errors** exit in production (`index.ts` L22ÔÇô24)

---

## Health endpoint security

`GET /health` is **public** (no auth) ÔÇö standard for load balancers. Returns version, git commit, DB status, migration count, upload provider ÔÇö **no secrets**. Returns **503** when degraded (`healthHttpStatus`).

---

## Security certification evidence

| Harness | Result |
|---------|--------|
| verify:financial security | 11/11 |
| verify:live RBAC steps | PASS |
| P14.3B security cert | 11/11 (historical, not re-run full doc) |

---

## Security findings

| ID | Finding | Severity |
|----|---------|----------|
| SEC-001 | No rate limit on authenticated API endpoints | Medium |
| SEC-002 | Upload POST lacks purpose/role-specific permission | Medium |
| SEC-003 | Dual route mount `/` and `/api/v1` ÔÇö ensure WAF covers both | Low |
| SEC-004 | `cert-upload-smoke` UUID failure blocks upload lifecycle proof | High |
| SEC-005 | HTTPS termination external ÔÇö app assumes HTTP behind proxy | Medium (ops) |
| SEC-006 | CORS single-origin ÔÇö staging must match exactly | Medium (ops) |

No security code changes in this audit.
