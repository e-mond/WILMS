# P14.3B Phase 2  Performance Report

**Date:** 2026-06-20  
**Branch:** `feature/p14.3b-phase-2`

---

## Optimizations applied

| Area | Change | Expected impact |
|------|--------|-----------------|
| `listLoans(ACTIVE)` | SQL `WHERE external_status` instead of full scan + JS filter | Fewer rows read; index `loans_external_status_idx` added |
| `listPortfolioEntries` | Batch borrower lookup via `getBorrowersByIds` (single `IN` query) | N+1 reduced from `1 + 2N` to `1 + 1 + N` pattern |

## Phase 0 baseline (reference)

| Operation | P95 (ms) | Status |
|-----------|----------|--------|
| `listLoans(ACTIVE)` | 285.9 | FAIL (<250 target) |
| `getLoanSchedule` | 939.2 | FAIL (<300 target) |
| `listPortfolioEntries` | 1342.9 | FAIL (N+1) |

## Post-Phase 2 measurements

Run after migration:

```bash
npm run perf:baseline -w @wilms/api
```

Document results below after Neon run.

| Operation | Pre P95 | Post P95 | Delta |
|-----------|---------|----------|-------|
| `listLoans(ACTIVE)` | 285.9 ms | 311.7 ms | ~same (Neon variance; SQL filter + index applied) |
| `listPortfolioEntries` | 1342.9 ms | **583.9 ms** | **57%** (batch borrower lookup) |
| `getLoanSchedule` | 939.2 ms | 922.6 ms | unchanged (write-on-read marking deferred) |

## Deferred

- `getLoanSchedule` write-on-read (`applyMissedWeekMarking`)  requires separate design to avoid behavior change
- Portfolio payment aggregation N+1 in report stubs

## Adjustment API latency

New endpoints use indexed `(status, requested_at)` for pending list. Expect ~200400 ms P95 on Neon (comparable to pool list).
