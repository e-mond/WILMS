# P14.5D ÔÇö Production Environment Matrix

**Date:** 2026-06-29  
**Important:** The P14.5D brief uses some variable names that **do not exist** in the WILMS codebase. This matrix lists **actual** names from `env.ts`, `validate-env.ts`, and frontend config.

---

## Brief name ÔåÆ actual name mapping

| Brief (P14.5D) | **Actual WILMS variable** | Platform |
|----------------|---------------------------|----------|
| `SESSION_SECRET` | **`WILMS_SESSION_SECRET`** | Railway |
| `NEXT_PUBLIC_API_URL` | **`NEXT_PUBLIC_API_BASE_URL`** (BFF path) + **`WILMS_API_UPSTREAM`** (server) | Vercel |
| `NEXTAUTH_URL` | **Not used** ÔÇö custom HMAC session via `/api/auth/login` | ÔÇö |
| `NEXTAUTH_SECRET` | **Not used** ÔÇö use `WILMS_SESSION_SECRET` on backend only | ÔÇö |
| `CLOUDINARY_URL` | **Not used** ÔÇö use `CLOUDINARY_CLOUD_NAME`, `CLOUDINARY_API_KEY`, `CLOUDINARY_API_SECRET` | Railway |
| `FRONTEND_URL` | **`WILMS_CORS_ORIGIN`** (backend) | Railway |

---

## Railway ÔÇö required variables

| Variable | Source | Confirmed | Notes |
|----------|--------|-----------|-------|
| `DATABASE_URL` | Neon dashboard | ÔÿÉ | `?sslmode=require` |
| `WILMS_SESSION_SECRET` | Generated (32+ chars) | ÔÿÉ | **Not** `SESSION_SECRET` |
| `NODE_ENV` | `production` | ÔÿÉ | |
| `PORT` | Railway injects | ÔÿÉ | Read by `env.ts` |
| `WILMS_API_HOST` | `0.0.0.0` | ÔÿÉ | Default in production (P14.5C) |
| `WILMS_CORS_ORIGIN` | Vercel production URL | ÔÿÉ | **Not** `FRONTEND_URL` |
| `UPLOAD_PROVIDER` | `cloudinary` | ÔÿÉ | |
| `CLOUDINARY_CLOUD_NAME` | Cloudinary | ÔÿÉ | |
| `CLOUDINARY_API_KEY` | Cloudinary | ÔÿÉ | |
| `CLOUDINARY_API_SECRET` | Cloudinary | ÔÿÉ | |
| `CLOUDINARY_FOLDER` | e.g. `wilms-prod` | ÔÿÉ | Optional |
| `WILMS_TRUST_PROXY` | `true` | ÔÿÉ | Behind Railway proxy |
| `WILMS_GIT_COMMIT` | CI inject | ÔÿÉ | Optional ÔÇö `/health` |

---

## Vercel ÔÇö required variables

| Variable | Expected | Confirmed | Notes |
|----------|----------|-----------|-------|
| `NODE_ENV` | `production` | ÔÿÉ | Platform default |
| `NEXT_PUBLIC_APP_URL` | `https://<vercel-domain>` | ÔÿÉ | |
| `NEXT_PUBLIC_API_BASE_URL` | `https://<vercel-domain>/api/wilms` | ÔÿÉ | **Not** `NEXT_PUBLIC_API_URL` |
| `WILMS_API_UPSTREAM` | `https://<railway-api-domain>` | ÔÿÉ | Server-only BFF upstream |
| `NEXT_PUBLIC_USE_MOCK` | `false` | ÔÿÉ | **Mandatory** |
| `NEXT_PUBLIC_DEMO_MODE` | unset / empty | ÔÿÉ | |
| `NEXT_PUBLIC_FORCE_DEMO_MODE` | unset | ÔÿÉ | |
| `NEXT_PUBLIC_API_DISABLED` | unset | ÔÿÉ | |
| `NEXT_PUBLIC_WILMS_ENV` | `production` | ÔÿÉ | Recommended |

---

## Unused / duplicate variables

| Variable | Status |
|----------|--------|
| `JWT_SECRET` | Does not exist |
| `SESSION_SECRET` | Does not exist ÔÇö use `WILMS_SESSION_SECRET` |
| `API_PORT` | Duplicate alias of `WILMS_API_PORT` / `PORT` |
| `MAIL_*`, `SMS_*` | Infrastructure only ÔÇö no V1 workflows |

---

## Localhost risk (production must not use)

| Location | Fallback | Mitigation |
|----------|----------|------------|
| BFF `route.ts` | `127.0.0.1:4000` dev only | Throws if `WILMS_API_UPSTREAM` missing in production |
| `env.ts` `corsOrigin` | `http://127.0.0.1:3000` | `validate-env.ts` requires explicit `WILMS_CORS_ORIGIN` in production |
| Smoke script | ÔÇö | Fails if URLs contain localhost or `http://` |

---

## Verification commands

```bash
# After Railway deploy
curl -fsS "https://<api>/health"

# After Vercel deploy (set env first)
WILMS_APP_URL=https://... WILMS_API_URL=https://... \
  WILMS_SMOKE_PASSWORD=... npm run smoke:production
```
