# P14.3A.4 ÔÇö Frontend Live Verification

**Phase:** P14.3A.4 Phase 3  
**Date:** 2026-06-09  
**Status:** Complete (evidence-based, smoke-level)

---

## Modes

| Mode | When | Data source |
|------|------|-------------|
| Mock (default dev) | `NODE_ENV=development` | In-memory / local mock services |
| API (live) | `NODE_ENV=production` + `NEXT_PUBLIC_API_BASE_URL` | BFF ÔåÆ backend ÔåÆ Neon |

Resolver: `apps/frontend/src/data-provider/types.ts` ÔåÆ `resolveDataProviderMode()`.

---

## Production Smoke Setup

```powershell
cd apps/frontend
$env:NODE_ENV="production"
$env:PORT="3010"
$env:WILMS_API_UPSTREAM="http://127.0.0.1:4000"
$env:NEXT_PUBLIC_API_BASE_URL="http://127.0.0.1:3010/api/wilms"
npm run start
```

**Startup evidence:**

```text
Ôû▓ Next.js 14.2.18
- Local:        http://localhost:3010
Ô£ô Ready in 1445ms
```

Backend concurrently running on `127.0.0.1:4000` with `[postgresql (Neon)]`.

---

## BFF Proxy Verification

| Check | URL | Result |
|-------|-----|--------|
| Health proxy | `GET /api/wilms/health` | **200** ÔÇö `{"data":{"status":"ok","service":"wilms-api"}}` |

Proxy route: `apps/frontend/src/app/api/wilms/[...path]/route.ts`  
Upstream: `WILMS_API_UPSTREAM` ÔåÆ `http://127.0.0.1:4000`

---

## Auth Route (Frontend ÔåÆ Backend ÔåÆ Neon)

`POST /api/auth/login` with seed credentials:

```json
{"email":"admin@wilms.demo","password":"DemoAdmin1!"}
```

**Response (session cookie set, Neon user):**

```json
{
  "user": {
    "id": "019ed644-4a3e-7b19-a5df-a58e149e9063",
    "role": "SUPER_ADMIN",
    "displayName": "Ama Boateng"
  },
  "expiresAt": 1782055892273
}
```

Direct backend login for comparison returned matching `SUPER_ADMIN` role and JWT token in `data.token`.

**Verdict:** Frontend auth route reaches live backend and Neon-seeded users.

---

## Build & Unit Tests

| Gate | Result |
|------|--------|
| `npm run build` | Ô£à 42 routes |
| `npm run test` | Ô£à 400/400 |

---

## E2E (mock data, CI)

```text
CI=1 PLAYWRIGHT_PORT=3011 FRONTEND_PORT=3011 npm run test:e2e
185 passed (14.2m)
1 skipped
exit 0
```

E2E validates UI against **mock** provider (default Playwright webServer). Not a substitute for full browser walkthrough in API mode.

---

## Gaps (honest)

| Gap | Detail |
|-----|--------|
| Full UI in API mode | Not browser-tested page-by-page in this phase |
| Default `.env` URL | User file uses `http://localhost:3000/` without `/api/wilms` |
| P14.3B pages | Loan pools, adjustments, reconciliation remain mock-only |
| Demo banner | Hidden in production API mode (expected) |

---

## Frontend Live Verdict

**BFF + auth live path verified** (Frontend ÔåÆ Backend ÔåÆ Neon for login and health).  
**Partial** ÔÇö comprehensive UI regression in API mode deferred; mock E2E remains primary UI gate.
