# P14.3B Phase 2  Financial Adjustments Implementation Report

**Date:** 2026-06-20  
**Branch:** `feature/p14.3b-phase-2`

---

## Summary

Implemented approval-gated financial adjustments with PostgreSQL persistence, ledger integration, audit wiring, and verification harness.

## Schema

| Table | Purpose |
|-------|---------|
| `financial_adjustments` | Pending/approved/rejected correction requests |
| `adjustment_reasons` | Standardized reason catalog (fee, interest, admin, balance, manual) |
| `adjustment_history` | Append-only before/after/delta trail |

Migration: `0004_financial_adjustments.sql`

## API

| Method | Path | Permission |
|--------|------|------------|
| GET | `/adjustments/pending` | `access-admin-portal` |
| GET | `/adjustments` | `access-admin-portal` |
| GET | `/adjustments/:id` | `access-admin-portal` |
| POST | `/adjustments` | `record-collections` |
| POST | `/adjustments/:id/approve` | `access-admin-portal` |
| POST | `/adjustments/:id/reject` | `access-admin-portal` |

## Transaction boundary (approve)

```text
BEGIN
  optimistic lock adjustment row
  update loan balance + lifecycle
  append ledger ADJUSTMENT entry
  optional pool ADJUSTMENT allocation
  WRITE_OFF  blacklist borrower
  append history (APPROVED + LEDGER_POSTED)
COMMIT
append audit (async)
```

## Files added

- `apps/backend/src/db/schema/financial-adjustments.ts`
- `apps/backend/src/domain/adjustment/*`
- `apps/backend/src/repositories/adjustment.repository.ts`
- `apps/backend/src/repositories/adjustment-history.repository.ts`
- `apps/backend/src/modules/adjustments/service.ts`
- `apps/backend/src/modules/adjustments/routes.ts`
- `apps/backend/src/verification/adjustment-checks.ts`
- `apps/backend/src/verification/run-adjustments.ts`
- `apps/backend/src/db/seed/seed-adjustments.ts`

## Out of scope (Phase 3+)

- Reversals engine
- Dedicated write-off service
- Reconciliation integration flags
