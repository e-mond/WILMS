# P14.5B  Pull Request Description

## Summary

- P14.5A.3 RC0 defect fixes (Cloudinary smoke UUIDv7, upload provider, reconciliation RBAC cert, Neon perf budgets)
- README synchronized to P14.5A.3 CONDITIONAL verdict
- P14.5A.2 release freeze audit docs and `phase-5a.3-evidence/` certification logs
- Repository cleanup: remove tracked coverage, test-results, and temp logs from git index
- Extend `.gitignore` to prevent regenerated artifact commits
- Add `apps/frontend/vercel.json` for npm-workspace monorepo builds on Vercel

## Vercel fix

**Root cause:** Next.js output is at `apps/frontend/.next`, but Vercel looked for `.next` at monorepo root.

**Fix:** Set Vercel **Root Directory** to `apps/frontend`. Install/build commands run from repo root via `vercel.json`.

## Test plan

- [x] `npm run type-check`  PASS
- [x] `npm run lint`  PASS
- [x] `npm run build`  PASS; `.next` at `apps/frontend/.next`
- [x] `npm run test`  **205/205** PASS
- [ ] Vercel production deploy (requires `vercel login` + dashboard env vars)
- [ ] Production smoke tests (requires deployed frontend + live backend URL)

## Vercel dashboard settings (required)

| Setting | Value |
|---------|-------|
| Root Directory | `apps/frontend` |
| Framework | Next.js |
| Install Command | `cd ../.. && npm ci` |
| Build Command | `cd ../.. && npm run build -w @wilms/frontend` |
| Output Directory | `.next` (default, relative to Root Directory) |

## Production environment variables (Vercel)

| Variable | Required |
|----------|----------|
| `NEXT_PUBLIC_APP_URL` | Production frontend URL |
| `NEXT_PUBLIC_API_BASE_URL` | `{APP_URL}/api/wilms` |
| `WILMS_API_UPSTREAM` | Live Express backend URL (not localhost) |
| `NEXT_PUBLIC_USE_MOCK` | `false` or unset |

Backend (`DATABASE_URL`, Cloudinary, `WILMS_SESSION_SECRET`, etc.) deploys separately from frontend.
