# P14.5A — Health Endpoint Audit

**Blocker:** RC-056 · B-005

## Before

`GET /health` returned static `{ status: 'ok', service: 'wilms-api' }` — no DB probe.

## After

**File:** `apps/backend/src/modules/health/health.service.ts`

Response includes: `status`, `service`, `version`, `gitCommit`, `uptimeSeconds`, `environment`, `timestamp`, `database`, `migrations`, `uploads`, `session`.

| Condition | HTTP |
|-----------|------|
| OK (DB connected or disabled in dev) | 200 |
| Degraded (DB down when configured, migration gap, prod upload invalid) | 503 |

## Tests

`apps/backend/src/tests/health/health.service.test.ts` — 2 tests PASS.

## Status

**Resolved** (code). Staging probe with real Neon recommended before production.
