# P14.3B Phase 5A — Production Configuration Audit

**Date:** 2026-06-26  
**Environment audited:** Local dev + `.env.example` / production examples  
**Verdict:** **CONDITIONAL** — local stack valid; production secrets incomplete

---

## Backend

| Area | Dev (actual) | Production (required) | Status |
|------|--------------|----------------------|--------|
| `NODE_ENV` | `development` | `production` | Documented |
| `DATABASE_URL` | Neon configured | Required | **PASS** (dev) |
| `WILMS_API_HOST` | `127.0.0.1` | `0.0.0.0` | Documented in `.env.production.example` |
| `WILMS_API_PORT` | `4000` | `4000` | **PASS** |
| `WILMS_CORS_ORIGIN` | `http://127.0.0.1:3000` | Production app URL | Must set at deploy |
| `UPLOAD_PROVIDER` | `local` (default) | `cloudinary` | **FAIL** — not configured |
| `CLOUDINARY_*` | Unset | Required for prod uploads | **FAIL** |
| `MAIL_PROVIDER` | `none` | `resend` (example) | Not validated |
| `SMS_PROVIDER` | `none` | `arkesel` (example) | Not validated |
| Health check | `GET /health` → 200 | Same | **PASS** |
| Logging | Console default | Platform logs | Accepted — no custom aggregator |

**Evidence:** `npm run cert:upload:env` → `active_provider: local`, `cloudinary_configured: false`

---

## Frontend

| Area | Dev | Production | Status |
|------|-----|------------|--------|
| `NEXT_PUBLIC_API_BASE_URL` | `http://127.0.0.1:3000/api/wilms` (`.env.local`) | `https://app…/api/wilms` | Documented |
| `WILMS_API_UPSTREAM` | `http://127.0.0.1:4000` | Internal API URL | Documented |
| Mock mode | `NEXT_PUBLIC_USE_MOCK=false` in `.env.local` | Auto `api` when `NODE_ENV=production` + URL | **PASS** (when configured) |
| `NEXT_PUBLIC_DEMO_MODE` | Unset | Must be unset/false | Documented |
| Proxy | `/api/wilms/[...path]` → upstream | Same pattern | **PASS** |
| Auth | `/api/auth/login` cookie | Same | **PASS** |

**Source files:** `apps/frontend/.env.production.example`, `next.config.mjs`, `data-provider/types.ts`

---

## Infrastructure

| Area | Status | Evidence |
|------|--------|----------|
| Migrations | 7 files, journal ordered | `_journal.json` |
| Database | Neon PostgreSQL | `cert:reconciliation:env` PASS |
| Storage (local) | `.wilms-uploads` disk | Upload cert PASS |
| Storage (Cloudinary) | Not configured | **FAIL** for production |
| Backups | Neon platform | **Accepted** — operator responsibility; not verified in repo |

---

## Secrets Handling

| Secret | In repo? | Location |
|--------|----------|----------|
| `DATABASE_URL` | `.env` (gitignored) | Root `.env` |
| `CLOUDINARY_API_SECRET` | Example only | `.env.production.example` placeholders |
| `RESEND_API_KEY` | Example only | Not in committed `.env` |

**PASS:** No secrets in committed source (grep verified in 4C Cloudinary review).

---

## Production vs Dev Gaps

1. **Cloudinary** — production example requires `UPLOAD_PROVIDER=cloudinary`; dev uses local.
2. **CORS** — must match deployed frontend origin.
3. **Main branch** — production deploy from `main` would exclude P14.3B until merge.
4. **Mail/SMS** — infrastructure stubs only; no workflow dependency for P14.3B financial features.

---

## Recommendations

| Priority | Action |
|----------|--------|
| Blocking | Merge P14.3B to release branch before production deploy |
| Blocking | Configure Cloudinary in deployment platform |
| High | Set `WILMS_CORS_ORIGIN` and `NEXT_PUBLIC_APP_URL` to production domains |
| Medium | Document Neon backup/restore procedure for operators |
| Low | Update cert probe migration list to include 0006 |
