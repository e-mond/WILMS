# P14.3B — Cloudinary Certification

**Date:** 2026-06-22  
**Status:** **CONDITIONAL** — code certified; live Cloudinary upload pending developer credentials

---

## Certification Matrix

| Check | Result | Evidence |
|-------|--------|----------|
| No hardcoded secrets | **PASS** | Grep `CLOUDINARY` in source — env vars only |
| Frontend excludes API secret | **PASS** | `apps/frontend/.env*.example` — no `CLOUDINARY_API_SECRET` |
| Type-check | **PASS** | `npm run type-check -w @wilms/api` |
| Env certification script | **PASS** | `npm run cert:upload:env -w @wilms/api` |
| Upload routes auth-gated | **PASS** | `routes.ts` L45 `requireAuth` |
| DB persistence wired | **PASS** | `upload.repository.ts` → `uploads` table |
| Local dev fallback | **PASS** | `UPLOAD_PROVIDER=local` default |
| Production env validation | **PASS** | Errors when cloudinary requested without creds in production |
| Live Cloudinary upload | **PENDING** | Awaiting developer credential insertion |
| Staging verification | **PENDING** | Awaiting deployment config |
| Production verification | **PENDING** | Awaiting deployment config |

---

## Validation Commands

```bash
# Type safety
npm run type-check -w @wilms/api

# Upload environment (secrets masked)
npm run cert:upload:env -w @wilms/api

# Frontend unit tests (upload utilities)
npm run test -w @wilms/frontend -- upload
```

---

## cert:upload:env Output (local, no Cloudinary creds)

```text
database: enabled
requested_provider: local
active_provider: local
cloudinary_configured: false
cloud_name: (unset)
api_key: (unset)
api_secret: (unset)
valid: PASS
```

---

## Upload Flow Verification Checklist

| Flow | Dev (mock) | API + local | API + Cloudinary |
|------|------------|-------------|------------------|
| Borrower photo | Mock store | `POST /uploads` → disk | Pending creds |
| Guarantor photo | Mock store | `POST /uploads` → disk | Pending creds |
| Registration attachment | Mock store | `POST /uploads` → disk | Pending creds |
| Staff profile | Mock store | `POST /uploads` → disk | Pending creds |
| Signature / thumbprint | Mock store | `POST /uploads` → disk | Pending creds |
| Document / expense receipt | Mock store | `POST /uploads` → disk | Pending creds |
| Delete upload | Mock store | `POST /uploads/:id/delete` | Pending creds |
| Signed direct upload | N/A | 404 (local) | `GET /uploads/signature` |

---

## Environment Profiles

| Profile | `UPLOAD_PROVIDER` | Expected `active_provider` |
|---------|-------------------|---------------------------|
| Local dev (default) | `local` | `local` |
| Local + Cloudinary | `cloudinary` + creds | `cloudinary` |
| Staging | `cloudinary` + creds | `cloudinary` |
| Production | `cloudinary` + creds | `cloudinary` |

---

## Known Limitations

- Upload transport is base64 `dataUrl` only — multipart not yet enabled
- In-memory metadata fallback when `DATABASE_URL` unset (local dev without Neon)
- Borrower `photoUrl` in API still uses `/uploads/:id/content` proxy path (redirects to CDN when Cloudinary)

---

## Next Steps

1. Developer inserts Cloudinary credentials into local/production env files
2. Re-run `npm run cert:upload:env -w @wilms/api` — expect `active_provider: cloudinary`
3. Manual smoke test: upload borrower photo in API mode
4. Deploy staging with `CLOUDINARY_FOLDER=wilms-staging`
5. Deploy production with `CLOUDINARY_FOLDER=wilms-prod`

**Ready for credential insertion. Do not share secrets in PR or chat.**
