# P14.3A.3 — Environment Loading Audit

**Phase:** P14.3A.3 Phase 2  
**Date:** 2026-06-09  
**Status:** Complete

---

## Prior State (P14.3A.2 finding)

**Verified:** Backend did **not** automatically load `.env` files. Developers had to export `DATABASE_URL` manually.

---

## Audit Results

| Mechanism | Frontend | Backend (before) | Backend (after) |
|-----------|----------|------------------|-----------------|
| `dotenv` | N/A (Next.js built-in) | ❌ Not used | ✅ `dotenv` package |
| `dotenv-flow` | ❌ | ❌ | ❌ Not needed |
| `@next/env` | ✅ Next.js internal | N/A | N/A |
| Manual shell export | — | Required | **Not required** |

---

## Implementation

**File:** `apps/backend/src/config/load-env.ts`

Load order (later files fill only **unset** keys):

1. `{monorepo}/.env`
2. Development: `apps/backend/.env`, `apps/backend/.env.local`, `{monorepo}/.env.local`
3. Production: `apps/backend/.env.production`, `{monorepo}/.env.production`

**Entry points importing load-env:**

| File | Purpose |
|------|---------|
| `src/index.ts` | API server startup |
| `src/db/seed/run-seed.ts` | Database seed |
| `src/verification/run-all.ts` | Financial harness |
| `drizzle.config.ts` | Migrations |

`loadEnvironment()` runs automatically on module import.

---

## Runtime Verification (2026-06-09)

```text
> tsx src/index.ts

◇ injected env (10) from ..\..\.env
WILMS API listening on http://127.0.0.1:4000 [postgresql (Neon)]
Upload provider: local | Mail: none | SMS: none
```

| Check | Result |
|-------|--------|
| `.env` loaded without manual export | ✅ |
| `DATABASE_URL` recognized | ✅ `[postgresql (Neon)]` |
| Provider config resolved | ✅ |
| Production override safety | ✅ dotenv skips existing process vars |

---

## Frontend Loading

Next.js automatically loads (in order):

- `.env`
- `.env.local`
- `.env.development` / `.env.production`
- `.env.development.local` / `.env.production.local`

No code changes required. Documented via `apps/frontend/.env*.example`.

---

## Developer Action

Copy example files → set `DATABASE_URL` → run `npm run dev:api`. No PowerShell `$env:` exports needed.
