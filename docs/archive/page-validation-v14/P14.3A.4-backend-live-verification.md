# P14.3A.4 — Backend Live Verification

**Phase:** P14.3A.4 Phase 2  
**Date:** 2026-06-09  
**Status:** Complete (evidence-based)

---

## Harness

**Script:** `npm run verify:live -w @wilms/api`  
**Source:** `apps/backend/src/verification/live-integration.ts`  
**Requires:** Running API on `127.0.0.1:4000` + `DATABASE_URL` (Neon)

---

## Session Result: 23/23 Passed

```text
P14.3A.4 Live Integration Verification
API base: http://127.0.0.1:4000
DATABASE_URL: configured

=== LIVE INTEGRATION RESULTS ===
  ✓ health-endpoint: status 200
  ✓ login-admin: token received
  ✓ login-approver: token received
  ✓ login-collector: token received
  ✓ login-officer: token received
  ✓ rbac-unauthenticated-loans: status 401
  ✓ rbac-collector-list-loans-denied: status 403
  ✓ rbac-approver-list-loans-denied: status 403 (requires view-financial-reports)
  ✓ loans-list-active: status 200, count=2
  ✓ borrowers-list: status 200
  ✓ reports-list: status 200
  ✓ neon-loans-persisted: loan rows=5
  ✓ neon-ledger-persisted: ledger rows=28
  ✓ loan-schedule-retrieval: status 200
  ✓ loan-progress-retrieval: status 200
  ✓ workflow-create-loan: status 201
  ✓ workflow-approve-loan: status 200
  ✓ workflow-disburse-loan: status 200
  ✓ workflow-loan-active-in-db: externalStatus=ACTIVE
  ✓ workflow-post-repayment: status 201
  ✓ workflow-payment-persisted: payments=1
  ✓ workflow-ledger-updated: ledger=3
  ✓ approver-user-in-db: (seed user present)

Total: 23/23 passed
```

---

## Endpoint Coverage

| Area | Method / Path | Actor | Status |
|------|---------------|-------|--------|
| Health | `GET /health` | none | 200 |
| Auth | `POST /auth/login` | demo users | 200 + token |
| RBAC deny | `GET /loans` | unauthenticated | 401 |
| RBAC deny | `GET /loans` | collector | 403 |
| RBAC deny | `GET /loans` | approver | 403 (by design) |
| Loans list | `GET /loans?status=ACTIVE` | super admin | 200 |
| Borrowers | `GET /borrowers` | super admin | 200 |
| Reports | `GET /reports` | super admin | 200 |
| Schedule | `GET /loans/:id/schedule` | super admin | 200 |
| Progress | `GET /loans/:id/progress` | super admin | 200 |
| Create loan | `POST /loans` | approver | 201 |
| Approve | `PATCH /loans/:id/approve` | approver | 200 |
| Disburse | `POST /loans/:id/disburse` | approver | 200 |
| Payment | `POST /payments` | collector | 201 |

---

## Neon Persistence Checks

Direct DB reads via Drizzle (same process as harness):

| Table | Assertion |
|-------|-----------|
| `loans` | ≥ 4 rows (seed + workflow) |
| `ledger_entries` | > 0 rows |
| Workflow loan | `externalStatus=ACTIVE` after disburse |
| Workflow payment | ≥ 1 payment row after collector post |
| Workflow ledger | ≥ 3 entries on workflow loan |

---

## Financial Harness (separate)

`npm run verify:financial -w @wilms/api` — **58/60** in same session:

| Failure | Detail |
|---------|--------|
| `loan-list-p95-ms` | p95=299.4ms (target <250ms) |
| `schedule-retrieval-p95-ms` | p95=1432.2ms (target <300ms) |

Concurrency checks **skipped** (no payable week on seed loan after prior repayments).

Unit (12/12) and security/RBAC (6/6) checks **passed**.

---

## Harness Corrections (P14.3A.4)

Initial run (17/22) failed due to **incorrect test assumptions**, not production defects:

1. **Approver cannot list loans** — `GET /loans` requires `view-financial-reports` (`packages/shared-rbac` + `loans/routes.ts`). Harness updated to assert 403 for approver and use admin for list/schedule/progress.
2. **Payment 422** — Fixed amount/date to match first payable schedule week (`loan_schedules.installment_amount` + `due_date`).

---

## Backend Live Verdict

**Backend ↔ Neon live integration verified** via HTTP harness and direct DB reads.  
Financial performance targets against remote Neon remain **open**.
