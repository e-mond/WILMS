# P14.5D — Pull Request Description

## Summary

Final production deployment phase for full-stack WILMS:

- **Automated production smoke tests** — `npm run smoke:production` (`production-smoke.ts`)
- **Railway migrate-on-start** — `start:prod` runs `drizzle-kit migrate` before Express
- **Environment matrix** — correct variable names (`WILMS_SESSION_SECRET`, not `SESSION_SECRET`; no NextAuth)
- **Rollback & incident guide**
- **Final production report** — Phase 1–14 audit with honest NOT GO-LIVE verdict until platforms deployed

## Architecture (unchanged from P14.5C)

Vercel (Next.js + BFF) → Railway (Express) → Neon + Cloudinary

## Pre-deploy env (see P14.5D-production-environment.md)

**Railway:** `DATABASE_URL`, `WILMS_SESSION_SECRET`, `WILMS_CORS_ORIGIN`, `CLOUDINARY_*`, `UPLOAD_PROVIDER=cloudinary`

**Vercel:** `NEXT_PUBLIC_API_BASE_URL`, `WILMS_API_UPSTREAM`, `NEXT_PUBLIC_USE_MOCK=false`

## Test plan

- [x] `npm run type-check`
- [x] `npm run test -w @wilms/api` (11/11)
- [ ] Deploy Railway → `/health` 200
- [ ] Deploy Vercel with upstream URL
- [ ] `WILMS_APP_URL=... WILMS_API_URL=... npm run smoke:production`

## Known limitations

- 16 mock-only admin/collector pages (V1 scope)
- CSRF not implemented
- Live deployment blocked on platform credentials

## Docs

- `P14.5D-final-production-report.md`
- `P14.5D-production-environment.md`
- `P14.5D-rollback-guide.md`
