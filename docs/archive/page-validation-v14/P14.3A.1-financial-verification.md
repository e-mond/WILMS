# P14.3A.1 — Financial Verification

**Phase:** P14.3A.1 Phase 1  
**Date:** 2026-06-09  
**Status:** Complete (unit harness executed; persisted checks require DATABASE_URL)

---

## Execution Evidence (2026-06-09)

```bash
npm run verify:financial -w @wilms/api
# Unit Checks: 12/12 passed
# DATABASE_URL not configured — no persisted record verification
```

---

## Methodology

Automated checks in `apps/backend/src/verification/unit-checks.ts` plus seed reconciliation in `db-checks.ts` (when DATABASE_URL configured).

```bash
npm run verify:financial -w @wilms/api
```

---

## Loan Calculations

| Check | Rule | Result | Evidence |
|-------|------|--------|----------|
| Weekly installment | `Math.floor(amount / weeks)` | ✅ 50000÷10 = 5000 | `calculations.ts` |
| Divisibility gate | `amount % weeks === 0` | ✅ Enforced at create | `assertDivisibleLoanAmount()` |
| Pesewas ↔ decimal | `(pesewas/100).toFixed(2)` | ✅ Round-trip 50000 | unit check |
| Approved = principal | Same at create | ✅ `insertLoan()` | `loan.repository.ts` |
| Disbursed amount | Set on disburse transition | ✅ Transactional | `disburseLoan()` |
| Outstanding balance | Decremented per repayment | ✅ `loanBalance` update | `postPayment()` |

### Remaining Principal / Interest

P14.3A scope: zero-interest loans. `interest_amount` and `penalty_amount` default to `0`. Remaining principal equals `loan_balance`. No separate interest accrual engine — **Safe** per P14.3A design boundary.

---

## Schedule Accuracy

| Check | Expected | Result |
|-------|----------|--------|
| Week count | = `durationWeeks` | ✅ 10 weeks for 50000/10 |
| Installment sum | = principal | ✅ 10 × 5000 = 50000 |
| Final installment | Equal weekly (no remainder absorption) | ✅ Divisibility enforced |
| Rounding | Floor division only | ✅ Matches frontend mock |

Per seed loan (DB check when available):

* `schedule-sum-equals-principal-{loan}` — each seed loan schedule total vs principal

---

## Payment Allocation

| Behavior | Spec / Frontend | Verified |
|----------|-----------------|----------|
| Partial payments | Rejected | ✅ unit: "Partial payments are not allowed" |
| Overpayment | Rejected | ✅ unit: "Overpayment is not allowed" |
| Payment day gate | Weekday must match loan `paymentDay` | ✅ unit check |
| Oldest obligation first | MISSED before PENDING due | ✅ week 1 allocated over week 2 |
| Advance payments | Rejected when no payable week | ✅ `validatePaymentSubmission()` |
| Final payment | Balance → 0 triggers COMPLETED | ✅ `postPayment()` lifecycle branch |

**Note:** Partial/overpayment rejection is by design (frontend parity), not partial allocation.

---

## Automated Results (2026-06-09)

```
Unit Checks: 12/12 passed
  ✓ weekly-installment-floor
  ✓ schedule-sum-equals-principal
  ✓ installment-count
  ✓ pesewas-decimal-round-trip
  ✓ rejects-partial-payment
  ✓ rejects-overpayment
  ✓ rejects-wrong-payment-day
  ✓ oldest-obligation-first
  ✓ valid lifecycle transitions (3)
  ✓ rejects-completed-to-active
```

---

## Findings

| ID | Finding | Severity | Action |
|----|---------|----------|--------|
| FV-001 | No interest accrual (by design) | Info | Documented for P14.3B |
| FV-002 | Admin fee gate not server-enforced | Needs Attention | Deferred P14.3B (P14.3A gap) |

No calculation defects requiring code changes.
