# P14.3B Phase 4C.4 — Concurrency Certification

**Status:** PASS  
**Command:** `npm run cert:reconciliation:concurrency -w @wilms/api`  
**Output:** `P14.3B-phase-4c4-concurrency-output.txt`

---

## Methodology

Parallel `submitReconciliation()` calls for the same `(collector, date)` with unique idempotency keys per attempt.

---

## Results (measured)

| Level | Successes | Persisted | History | Duplicate Rej | Other | Pass |
|-------|-----------|-----------|---------|---------------|-------|------|
| 10 | 1 | 1 | 1 | 9 | 0 | YES |
| 25 | 1 | 1 | 1 | 24 | 0 | YES |
| 50 | 1 | 1 | 1 | 49 | 0 | YES |

**Exit code:** 0

---

## Interpretation

- Exactly **one** submission succeeds per collector+date.
- Duplicate attempts fail via DB unique index (`DUPLICATE:`) — classified as duplicate rejections.
- No validation or integrity violations (`otherRejections=0`).

---

## Known Limitations

- Test uses service layer concurrency (not HTTP). HTTP path shares same service + DB constraints.
