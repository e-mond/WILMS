# P14.5A.2 — Security Audit

**Scope:** Authentication, sessions, transport headers, uploads, secrets, env validation  
**Method:** Static code review + cert script re-run where noted

---

## Authentication

| Control | Implementation | Status |
|---------|----------------|--------|
| Password hashing | bcrypt via `lib/password.ts` | ✅ |
| Login validation | Zod `loginApiSchema` | ✅ |
| Failed login audit | `appendAuditEntry` in auth routes | ✅ |
| Demo user fallback | `DEMO_USERS` when no DATABASE_URL | ⚠️ Dev only |
| Session token | HMAC-signed JWT-like token `encodeSessionToken` | ✅ |
| Session duration | `WILMS_SESSION_DURATION_MS` / env | ✅ |

---

## Sessions & cookies

| Control | Location | Status |
|---------|----------|--------|
| Frontend session cookie | `lib/auth/cookies.ts`, BFF login route | ✅ httpOnly pattern |
| Middleware expiry | `parseSessionCookieState` → redirect `/session-expired` | ✅ |
| Backend session parse | `optionalAuth` / `requireAuth` middleware | ✅ |
| Logout | BFF clears cookie | ✅ |

---

## Transport & headers

| Control | Location | Status |
|---------|----------|--------|
| Helmet | `http/app.ts` L42–47 | ✅ CSP enabled in production |
| CORS | Single origin `env.corsOrigin`, credentials true | ✅ — must set per deploy |
| Trust proxy | `WILMS_TRUST_PROXY` + hops | ⚠️ Off by default |
| JSON body limit | 15mb (upload base64) | ✅ intentional |
| Rate limiting | Login only — 20/15min (`login-rate-limit.ts`) | ⚠️ No global API rate limit |

---

## Upload security

| Control | Status |
|---------|--------|
| MIME allowlist | ✅ `UPLOAD_ALLOWED_MIME_TYPES` |
| Size limit | ✅ `UPLOAD_MAX_SIZE_BYTES` |
| Provider validation | ✅ `validateUploadEnvironment()` |
| Owner-based access | ✅ `assertUploadAccess` on GET/delete |
| Cloudinary signed uploads | ✅ `/uploads/signature` |
| Multipart upload | ❌ Not implemented — dataUrl only |

**cert:upload:env:** PASS (cloudinary configured, secrets redacted in output)  
**cert:upload:smoke:** FAIL — `invalid input syntax for type uuid: "cert-smoke"` at `upload.repository.ts` — harness passes non-UUID `entityId`

---

## Secrets handling

| Item | Status |
|------|--------|
| `.env` in `.gitignore` | ✅ |
| Example files stripped of real creds | ✅ commit `169717b` |
| Env validation no secret logging | ✅ `validate-env.ts` |
| Local `.env` contains live Cloudinary + Neon credentials | ⚠️ Developer hygiene — not in git |

---

## Environment validation

`validateEnvironment()` (`config/validate-env.ts`):

- Production requires: `DATABASE_URL`, `WILMS_CORS_ORIGIN`, non-default `WILMS_SESSION_SECRET`
- Upload provider validation merged from upload env module
- **Warnings** do not block dev startup; **errors** exit in production (`index.ts` L22–24)

---

## Health endpoint security

`GET /health` is **public** (no auth) — standard for load balancers. Returns version, git commit, DB status, migration count, upload provider — **no secrets**. Returns **503** when degraded (`healthHttpStatus`).

---

## Security certification evidence

| Harness | Result |
|---------|--------|
| verify:financial security | 11/11 |
| verify:live RBAC steps | PASS |
| P14.3B security cert | 11/11 (historical, not re-run full doc) |

---

## Security findings

| ID | Finding | Severity |
|----|---------|----------|
| SEC-001 | No rate limit on authenticated API endpoints | Medium |
| SEC-002 | Upload POST lacks purpose/role-specific permission | Medium |
| SEC-003 | Dual route mount `/` and `/api/v1` — ensure WAF covers both | Low |
| SEC-004 | `cert-upload-smoke` UUID failure blocks upload lifecycle proof | High |
| SEC-005 | HTTPS termination external — app assumes HTTP behind proxy | Medium (ops) |
| SEC-006 | CORS single-origin — staging must match exactly | Medium (ops) |

No security code changes in this audit.
