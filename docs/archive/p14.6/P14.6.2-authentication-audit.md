# P14.6.2 — Production Authentication Audit

**Date:** 2026-06-30  
**GATE 2:** **FAIL** — live verification not completed  
**Mode:** Read-only — no user modifications, no reseed, no password rotation

---

## Production API baseline

`/health` at `2026-06-30T10:54:10Z`: **v0.2.1** (P14.5G runtime). Auth stack is CSRF BFF + HMAC session (P14.5G+).

---

## 1. User audit (database)

| Field | Status |
|-------|--------|
| Production `users` table query | **RUN** — `npx tsx scripts/audit-prod-users.mjs` (exit 0, `2026-06-30T12:19:01Z`) |
| Demo users in seed code | Documented below |
| Production `*@wilms.production` users | **None returned** (query scope: demo emails) |
| `last_login_at` | **Not in script output** |

### Live production users (Neon — read-only)

```json
[
  { "email": "admin@wilms.demo", "role": "SUPER_ADMIN", "status": "ACTIVE" },
  { "email": "collector@wilms.demo", "role": "COLLECTOR", "status": "ACTIVE" },
  { "email": "officer@wilms.demo", "role": "REGISTRATION_OFFICER", "status": "ACTIVE" },
  { "email": "approver@wilms.demo", "role": "APPROVER", "status": "ACTIVE" },
  { "email": "auditor@wilms.demo", "role": "AUDITOR", "status": "ACTIVE" }
]
```

All five seed demo accounts exist and are **ACTIVE**. No suspended or missing roles detected in DB.

### Seed definition (`apps/backend/src/seed/demo-users.ts`)

| Email | Role | Seed password | Type |
|-------|------|---------------|------|
| admin@wilms.demo | SUPER_ADMIN | DemoAdmin1! | demo |
| collector@wilms.demo | COLLECTOR | DemoCollect1! | demo |
| officer@wilms.demo | REGISTRATION_OFFICER | DemoOfficer1! | demo |
| approver@wilms.demo | APPROVER | DemoApprove1! | demo |
| auditor@wilms.demo | AUDITOR | DemoAudit1! | demo |

**Operator read-only SQL (template):**

```sql
SELECT email, role, status, created_at, updated_at, last_login_at
FROM users
ORDER BY email;
```

---

## 2. Login verification (live)

**Attempted:** `npm run smoke:production` and Node auth probe — **both hung** with no output (>255s).

| Role | Expected | Live result |
|------|----------|-------------|
| SUPER_ADMIN | 200 + session | **NOT VERIFIED** |
| COLLECTOR | 200 + session | **NOT VERIFIED** |
| APPROVER | 200 + session | **NOT VERIFIED** |
| REGISTRATION_OFFICER | 200 + session | **NOT VERIFIED** |
| AUDITOR | 200 + session | **NOT VERIFIED** |

### Partial evidence

| Test | Result | Source |
|------|--------|--------|
| `GET /login` | 200 | WebFetch 2026-06-30 |
| `GET /api/auth/csrf` | 200 `{"ok":true}` | WebFetch P14.6.1A |
| P14.5G historical smoke | 15/15 PASS | prior audit on v0.2.1 |

### Failure-mode tests (not run live)

| Case | Expected status | Expected body |
|------|-----------------|---------------|
| Wrong password | 401 | `Invalid email or password.` |
| Unknown email | 401 | same (no enumeration) |
| Disabled / SUSPENDED | 401 | same |
| Missing CSRF | **403** | `Invalid CSRF token.` |

Code references: `apps/frontend/src/app/api/auth/login/route.ts`, `apps/backend/src/modules/auth/routes.ts`

---

## 3. Session verification (design)

| Control | Expected | Code evidence |
|---------|----------|---------------|
| HttpOnly `wilms_session` | Yes | `getSessionCookieOptions()` |
| Secure (production) | Yes | `cookies.ts` |
| SameSite | Lax | `cookies.ts` |
| CSRF `wilms_csrf` + `x-wilms-csrf` | Yes | P14.5G BFF |
| Logout clears session | Yes | `/api/auth/logout` |
| Session expiry | `expiresAt` in response | `auth/routes.ts` |
| Remember login | Not implemented | — |

**Live cookie header inspection:** **NOT RUN**

---

## 4. RBAC verification

| Test | Status |
|------|--------|
| Role home redirect | **NOT RUN** |
| Unauthorized API → 401 | **NOT RUN** (historical smoke PASS) |
| Wrong role → 403 | **NOT RUN** |
| 403 not 500 on forbidden routes | **NOT RUN** |

---

## 5. Security message audit (code review)

| Requirement | Status |
|-------------|--------|
| No SQL in error responses | **PASS** (AppError messages) |
| No stack traces to client | **PASS** (Express error handler) |
| No internal IDs in login errors | **PASS** (generic 401 message) |
| No user enumeration | **PASS** (same message for missing user / bad password) |

---

## GATE 2 verdict

| Requirement | PASS? |
|-------------|-------|
| User DB audit | **PASS** (5/5 demo users ACTIVE in Neon) |
| 5-role login live | **FAIL** (not executed) |
| Session live verification | **FAIL** (not executed) |
| RBAC live verification | **FAIL** (not executed) |

**GATE 2: FAIL — WAIT**

Authentication **cannot be certified** until:

1. Railway runtime synced to v0.2.2
2. `npm run smoke:production` completes with 0 failures
3. Optional: DB user audit with read-only Neon access

**Note:** Auth may still work on v0.2.1 (historical 15/15), but P14.6.2 requires verification **after** full production sync.
