# P14.6 — Bundle Optimization Report

**Release:** v0.2.2  
**Branch:** `release/p14.6-cd-sync-ux`  
**Date:** 2026-06-30

---

## Budget Enforcement

| Script | Threshold | CI |
|--------|-----------|-----|
| `bundle:budget-check` | JS 350 KB gzip, CSS 100 KB gzip | ✅ `ci.yml` validate job |
| `perf:budget-check` | Delegates to bundle check | ✅ `ci.yml` validate job |

Chunk warn threshold: 200 KB gzip per file (logged, non-blocking).

---

## Measured (v0.2.2 build)

| Metric | Value | Budget | Status |
|--------|-------|--------|--------|
| First-load JS (gzip) | **168.5 KB** | 350 KB | PASS |
| CSS (gzip) | **8.1 KB** | 100 KB | PASS |
| Shared First Load JS | **87.8 KB** | — | Stable vs v0.2.1 |

First-load chunk pattern filters: `main-app-`, `webpack-`, `framework`, `polyfills-`, shared hashes — excludes lazy route chunks.

---

## Existing Optimizations (unchanged)

| Technique | Location |
|-----------|----------|
| `optimizePackageImports: ['lucide-react']` | `next.config.mjs` |
| Mock vs prod webpack alias | `next.config.mjs` |
| `transpilePackages` for shared monorepo packages | `next.config.mjs` |
| Dynamic import for heavy export/PDF | Approver routes (lazy) |

---

## v0.2.2 Delta

| Change | Bundle impact |
|--------|---------------|
| Offline queue toasts | Negligible |
| `loading.tsx` segments | Shared spinner — no new chunk |
| Sync API integration | Server-side only |
| CSRF helpers | No change from v0.2.1 |

---

## Recommendations (deferred)

| Item | Expected benefit |
|------|------------------|
| Dynamic import jsPDF on approver export | Reduce `/approver/pending/[id]` first load (~591 KB) |
| `React.memo` on report table rows | Measure before applying |

---

## Verdict

**PASS** — budgets enforced in CI; shared chunks 87.8 KB; total first-load gzip 168.5 KB well under limit.
