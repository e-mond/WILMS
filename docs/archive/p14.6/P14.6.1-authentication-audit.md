# P14.6.1 — Production Authentication Audit

**Date:** 2026-06-30  
**Production baseline:** v0.2.1 (P14.5G) — per `/health`  
**Status:** **PARTIAL** — STOP GATE 2 blocked on DB audit + full role matrix until deploy sync

---

## 2.1 User verification

| Item | Status | Evidence |
|------|--------|----------|
| Query production `users` table | **NOT RUN** | No `DATABASE_URL` in this environment — spec: do not modify users |
| Expected accounts | Documented below | Seed + prior evidence |

**Documented accounts** (from `apps/backend/src/seed/demo-users.ts`, prior production seed evidence):

| Email | Role | Password (seed) | Expected in prod |
|-------|------|-----------------|------------------|
| admin@wilms.demo | SUPER_ADMIN | DemoAdmin1! | Likely ACTIVE (P14.5G baseline) |
| collector@wilms.demo | COLLECTOR | DemoCollect1! | Likely ACTIVE |
| officer@wilms.demo | REGISTRATION_OFFICER | DemoOfficer1! | Likely ACTIVE |
| approver@wilms.demo | APPROVER | DemoApprove1! | Likely ACTIVE |
| auditor@wilms.demo | AUDITOR | DemoAudit1! | Likely ACTIVE |

`*@wilms.production` accounts: only if `rotate-production-users.mjs` was executed (not verified).

---

## 2.2 Login verification

| Test | Method | Result |
|------|--------|--------|
| BFF CSRF + login flow | P14.5G smoke 15/15 (historical) | **PASS** on v0.2.1 deploy |
| Live re-run `smoke:production` | Shell timeout this session | **NOT RUN** |
| Wrong password → secure message | Code: `login/route.ts` → 401 generic message | **Expected PASS** |
| User enumeration | Same 401 message for unknown email | **Expected PASS** |

**Production login URL:** `https://wilms.vercel.app/login` — HTTP 200 (WebFetch 2026-06-30).

---

## 2.3 Session verification (v0.2.1 production)

| Control | Expected | Evidence |
|---------|----------|----------|
| HttpOnly `wilms_session` | Yes | P14.5G smoke `session-cookie-httponly` |
| Secure (production) | Yes | `cookies.ts` `secure: NODE_ENV === 'production'` |
| SameSite | Lax | `cookies.ts` |
| CSRF `wilms_csrf` + header | Yes (P14.5G+) | BFF routes + smoke CSRF checks |
| Logout clears session | Yes | `useLogout.ts` + BFF logout |

---

## 2.4 RBAC verification

| Test | Status |
|------|--------|
| Role home paths | **NOT RE-RUN** live — historical smoke PASS |
| API 401 unauthenticated | Historical smoke `rbac-unauthenticated-api` |
| API 403 wrong role | **NOT RE-RUN** this session |

---

## 2.5 Database authentication audit

**NOT EXECUTED** — requires Neon read-only access. Operator query template:

```sql
SELECT email, role, status, password_hash IS NOT NULL AS has_hash,
       created_at, updated_at
FROM users
ORDER BY email;
```

Classify: `*@wilms.demo` vs `*@wilms.production`, `ACTIVE` vs `SUSPENDED`.

---

## 2.6 Railway log verification

**NOT EXECUTED** — requires Railway dashboard/CLI access.

---

## STOP GATE 2

**CONDITIONAL FAIL** for full acceptance:

- Auth **likely works** on current v0.2.1 (prior 15/15 smoke).
- **Cannot sign off** without: live smoke re-run, DB user audit, Railway login logs.
- **Blocked upstream** by STOP GATE 1 (P14.6 not on production).

**If login fails after v0.2.2 deploy:** halt and root-cause before cleanup or tagging.
