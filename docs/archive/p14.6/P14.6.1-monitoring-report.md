# P14.6.1 — Monitoring Verification

**Date:** 2026-06-30  
**Status:** Documented recommendations (dashboard access not available in session)

---

## Verified via API

| Check | Status | Evidence |
|-------|--------|----------|
| Health endpoint exists | ✅ | `GET /health` → 200, `status: ok` |
| DB connectivity in health | ✅ | `database.connected: true` |
| Upload provider in health | ✅ | `cloudinaryConfigured: true` |

---

## Requires operator dashboard

| Check | Platform | Recommendation |
|-------|----------|----------------|
| 404 rate alerts | Vercel | Enable Log Drains or integrate observability |
| 500 rate alerts | Railway + Vercel | Alert on 5xx spike > threshold |
| External uptime | Third-party | Poll `https://wilms.vercel.app/login` + `/health` every 5m |
| CPU / memory | Railway | Set alert > 80% sustained |
| DB connections | Neon | Monitor pool usage in Neon console |
| Slow queries | Neon | Enable query insights |
| Cloudinary failures | Cloudinary | Dashboard upload error metrics |
| Log aggregation | Railway + Vercel | Document dual-console procedure (see `P14.6-monitoring-verification.md`) |

---

## Verdict

Monitoring **partially operational** (health only). Formal alerting **not verified** — recommendations documented, not implemented per P14.6.1 scope.
