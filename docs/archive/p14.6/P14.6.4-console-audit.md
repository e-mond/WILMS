# P14.6.4 — Console & Network Audit

**Date:** 2026-06-30  
**Status:** **PARTIAL** — classification complete for known issues; full DevTools sweep pending final test run

---

## Scope

- Console errors, warnings, hydration issues
- Network 404/401/500 on primary workflows
- Classification: **Application** / **Environment** / **Extension** / **Expected**

Environments: local dev (`npm run dev`) + production (`https://wilms.vercel.app`).

---

## Findings classification

### Application — fixed in P14.6.4

| Symptom | URL / surface | Root cause | Fix | Evidence |
|---------|---------------|------------|-----|----------|
| `TypeError: … toISOString is not a function` | Settings → Users (`GET /settings/users`) | `lastLoginAt` rendered without null/type guard when DB row absent or non-Date | `formatLastLoginLabel()` guards `null`/`undefined`; maps via `row.lastLoginAt?.toISOString()` | `apps/backend/src/modules/settings/service.ts` (`formatLastLoginLabel`, `mapUserRowToSettingsRecord`) |
| HTTP **404** on admin/collector API paths | Dashboard, groups, settings, notifications, search, etc. | Frontend `apiClient` paths had no matching Express routes in API mode | 14 new backend modules + matrix script | `scripts/p14-6-4-api-matrix.mjs` — 108/108 matched |
| Daily collection report empty / contract mismatch | `/reports/daily-collection` | Route returned stub; frontend expected `summary` + `rows` | `buildDailyCollectionReport()` wired in reports route | `apps/backend/src/modules/reports/routes.ts`, `apps/backend/src/domain/reports/daily-collection.ts` |

### Application — fixed in P14.6.3 (carried forward)

| Symptom | Classification | Fix |
|---------|----------------|-----|
| BFF `POST /api/auth/login` → **401** while Railway `/auth/login` → **200** | Deployment / Application | Direct upstream call in `authenticateCredentials()` | `apps/frontend/src/lib/auth/authenticate.ts` — see [P14.6.3-authentication-recovery.md](./P14.6.3-authentication-recovery.md) |
| `apiClient` 401 redirect on `/login` | Expected → fixed (P14.6) | Public-path guard on unauthorized handler | `P14.6-console-error-triage.md` Error Set A |

### Environment / Extension — noise (no code change)

| Symptom | Classification | Evidence |
|---------|----------------|----------|
| `Unchecked runtime.lastError: A listener indicated an asynchronous response…` | **Extension noise** | No `chrome.runtime` usage in `apps/frontend/src`; signature matches browser extension messaging — `P14.6-console-error-triage.md` Error Set B |
| `runtime.lastError` on production login | **Environment** | `P14.6.1-console-audit.md` |

### Expected behavior

| Symptom | Classification | Notes |
|---------|----------------|-------|
| `GET /api/wilms/loans` without session → **401** | **Expected** | RBAC — not a login-page bug |
| `GET /api/auth/csrf` without prior cookie | **200** | CSRF bootstrap |
| PWA `beforeinstallprompt` handler | **Expected** | `PwaInstallBanner` — not an error |

---

## 404 inventory (before → after)

| Era | Orphan frontend paths | Live-mode result |
|-----|----------------------|------------------|
| Pre-P14.6.4 | 16+ path families documented in `P14.5A.2-frontend-audit.md`, `P14.4B-production-blockers.md` (RC-061) | HTTP 404 on admin/collector surfaces |
| Post-P14.6.4 | **0** unmatched `apiClient` calls | Matrix PASS 108/108 |

Remaining **404** risk: static assets during parallel `dev` + `build` (documented in `runtime-build-health-v2.md`) — **dev-only**, not production.

---

## Pending manual verification

| Category | Status |
|----------|--------|
| Full incognito production navigation | **NOT RUN** this session |
| React hydration warnings | **NOT RUN** |
| Unhandled promise rejections | **NOT RUN** |
| Network tab: no localhost/mock in production | Covered by smoke tests; manual audit pending |

---

## Classification summary

| Class | Count (classified) | Action |
|-------|-------------------|--------|
| Application (fixed) | 3 | Verify in final test run |
| Application (prior fix) | 2 | Already deployed P14.6.3 |
| Extension / Environment | 2 | Ignore in incognito |
| Expected | 3 | Document only |
| Pending full audit | Primary workflows | Run after P14.6.4 deploy |

---

## Verdict

**CONDITIONAL PASS** — Known application defects (TypeError date, API 404s, daily-collection contract) addressed in codebase. Full browser console certification deferred to final P14.6.4 test run.
