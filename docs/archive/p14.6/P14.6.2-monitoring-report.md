# P14.6.2 — Monitoring Verification Report

**Date:** 2026-06-30  
**Mode:** Document recommendations only — no dashboard changes

---

## Data collected

| Source | Access | Result |
|--------|--------|--------|
| Railway logs | No CLI/dashboard token | **NOT READ** |
| Vercel logs | No CLI/dashboard token | **NOT READ** |
| Railway CPU / memory | No dashboard | **NOT AVAILABLE** |
| Neon database metrics | No dashboard | **NOT AVAILABLE** |
| Cloudinary usage | No dashboard | **NOT AVAILABLE** |
| `/health` probe | Public API | **OK** on v0.2.1 — see health report |

### Live health snapshot (`2026-06-30T10:54:10Z`)

| Signal | Value | Concern |
|--------|-------|---------|
| `status` | ok | — |
| `database.connected` | true | — |
| `migrations` | 7/7 | **Drift** — expect 8/8 |
| `uploads.valid` | true | — |
| `uptimeSeconds` | 37422 | Stale deploy (~10h) |
| `version` | 0.2.1 | **Drift** |

---

## 404 / 500 monitoring

| Check | Status |
|-------|--------|
| Railway error rate | **NOT MEASURED** |
| Vercel function errors | **NOT MEASURED** |
| Known 404 routes | **NOT AUDITED** live |

---

## Alerting

| Capability | Status |
|------------|--------|
| Railway deploy failure alerts | **Unknown** — post-merge deploy may have failed silently |
| Health check monitoring | **Not configured in repo** |
| Uptime external probe | **Not verified** |

---

## Recommendations (do not execute here)

1. **Railway:** Enable deploy notifications; inspect logs for deployment `5251439530` (08:59 UTC) for build/migration failure.
2. **Set `WILMS_GIT_COMMIT`** on Railway — enables `/health` SHA drift detection.
3. **External uptime:** Monitor `GET /health` for `version`, `migrations.applied`, `gitCommit`.
4. **Alert on drift:** Fire if `version != 0.2.2` or `gitCommit == null` after release.
5. **Vercel:** Confirm production alias always points to latest successful `main` deployment.
6. **Neon:** Read-only connection pool / latency dashboard before stakeholder rollout.
7. **Cloudinary:** Monitor upload quota and error rate on receipt uploads.
8. **Post-redeploy:** Run `npm run smoke:production` on schedule (weekly) with failure webhook.

---

## Verdict

**FAIL** — Monitoring not verified. Recommendations documented only.

Cannot certify operational readiness without Railway log review of failed/stale deploy.
