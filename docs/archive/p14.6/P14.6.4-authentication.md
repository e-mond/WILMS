# P14.6.4 — Authentication Verification

**Date:** 2026-06-30  
**Status:** **PASS** (inherits P14.6.3; no auth regressions introduced by P14.6.4 API modules)

---

## Reference: P14.6.3 recovery

Full auth recovery documented in [P14.6.3-authentication-recovery.md](./P14.6.3-authentication-recovery.md).

| Layer | Finding | Fix |
|-------|---------|-----|
| Railway `POST /auth/login` | **200** — credentials valid | — |
| BFF `POST /api/auth/login` (before fix) | **401** — all roles | `authenticateCredentials()` called upstream directly |
| CSRF on BFF | **403** without token | Correct behavior |

**Fix file:** `apps/frontend/src/lib/auth/authenticate.ts` — bypasses `/api/wilms` proxy for server-side login (CSRF cookie not forwarded through proxy).

**Production:** Vercel deploy with BFF fix; see P14.6.3 production acceptance.

---

## P14.6.4 impact on authentication

| Check | Result | Notes |
|-------|--------|-------|
| New modules bypass auth? | **No** | All business routers use `requireAuth` (e.g. `settings/routes.ts`, `collectors/routes.ts`) |
| New public endpoints? | **No** | Only `health` + `auth` routers mounted outside permission gates |
| Session format changed? | **No** | HttpOnly `wilms_session` unchanged |
| CSRF on mutations | **Unchanged** | BFF CSRF for `/api/auth/*` |

---

## Live test matrix (P14.6.3 baseline — re-validate after P14.6.4 deploy)

Source: `scripts/p14-6-3-auth-probe.mjs` (referenced in P14.6.3 doc).

| Test | Expected | P14.6.3 |
|------|----------|---------|
| `GET /api/auth/csrf` | **200**, `wilms_csrf` set | PASS |
| Login without CSRF | **403** | PASS |
| SUPER_ADMIN login | **200**, `wilms_session` | PASS |
| COLLECTOR login | **200** | PASS |
| APPROVER login | **200** | PASS |
| REGISTRATION_OFFICER login | **200** | PASS |
| AUDITOR login | **200** | PASS |
| Wrong password | **401** (generic) | PASS |
| Unknown email | **401** (same message) | PASS |
| Direct Railway login | **200** | PASS |

---

## New module permission matrix (sample)

All new P14.6.4 routes require authenticated session + domain permission:

| Module | Permission gate (examples) | File |
|--------|---------------------------|------|
| `settings` | `MANAGE_SYSTEM_SETTINGS`, user/role CRUD permissions | `apps/backend/src/modules/settings/routes.ts` |
| `collectors` | `VIEW_ALL_COLLECTORS` | `apps/backend/src/modules/collectors/routes.ts` |
| `reports` | `VIEW_REPORTS` | `apps/backend/src/modules/reports/routes.ts` |
| `risk-flags` | `VIEW_RISK_FLAGS` (via middleware) | `apps/backend/src/modules/risk-flags/routes.ts` |

---

## Security checks (unchanged)

| Requirement | Status |
|-------------|--------|
| No SQL in errors | PASS |
| No stack traces to client | PASS |
| No user enumeration on login | PASS |
| Demo users ACTIVE in DB | PASS — `P14.6.3-database-verification.md` |

---

## Verdict

**PASS** — Authentication stack unchanged and verified in P14.6.3. P14.6.4 adds only authenticated business routes. Re-run auth probe after final P14.6.4 production deploy as regression check.
