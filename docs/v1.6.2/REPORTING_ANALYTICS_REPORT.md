# Reporting & Analytics Report (v1.6.2)

## New reports

| Report | Route |
|--------|-------|
| Write-offs | `/reports/write-offs` → `GET /reports/write-offs` |
| Aging analysis | `/reports/aging-analysis` → `GET /reports/aging-analysis` |

## Existing retained

Daily collection, loan portfolio, defaulters, collector performance, group risk, financial ledger, audit log.

## Export

Client-side PDF/Excel/CSV via existing WILMS export engines. Scheduled email delivery remains a follow-up (hub still documents empty scheduledReports).
